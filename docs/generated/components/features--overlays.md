<!-- GENERATED by scripts/gen-consumer-docs.mjs — do not edit. -->

# `features/overlays`

Any application modal — dialog, sheet or confirmation — including its async confirm lifecycle.

## Selection

- **Use when:** Any application modal — dialog, sheet or confirmation — including its async confirm lifecycle.
- **Avoid when:** The panel is non-modal and anchored to its trigger.
- **Consider instead:** `base/popover`, `features/async-preview`
- **Stability:** stable.

## Import

```tsx fragment — import-only example
import { ActionDialog } from "themelia-ui/features/overlays"
import "themelia-ui/features/overlays.css"
```

## Composition

This family composes `base/alert-dialog`, `base/buttons`, `base/dialog`, `base/display`, `base/feedback`, `base/overlay`, `base/sheet`.

Application policy—routing, fetching, persistence, permissions, and translation—stays
outside the package and arrives through the public props, callbacks, slots, or accessors below.

## Public declarations

### `ActionDialog`

Kind: callable.

```text
({ width, surfaceStyle, ...props }: ActionDialogProps) => import("react").JSX.Element
```

### `ActionDialogProps`

Kind: interface.

Extends: `OverlayBaseProps`, `OverlayActionProps`, `OverlayEmphasisProps`.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `width` | no | `OverlayWidth` | Maximum width. A CSS length, or one of the kit's steps. |
| `trigger` | no | `ReactNode` | The element that opens it. Supply this OR `open`, not both. |

### `ActionSheet`

Kind: callable.

```text
({ side, size, length, inset, modality, showFooter, ...props }: ActionSheetProps) => import("react").JSX.Element
```

### `ActionSheetProps`

Kind: interface.

Extends: `OverlayBaseProps`, `OverlayActionProps`, `OverlayEmphasisProps`.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `side` | no | `SheetSide` |  |
| `size` | no | `string` | Cross-axis extent: a side panel's width. |
| `length` | no | `string` | How far it runs along its edge; `inset` sets the gap from the viewport. See base/sheet. |
| `inset` | no | `boolean \| string` |  |
| `showFooter` | no | `boolean` |  |
| `modality` | no | `"modal" \| "trap-focus" \| "non-modal"` | `modal` traps focus, locks scroll and makes the page inert; `non-modal` sits beside<br>the app (inspectors, filter rails); `trap-focus` is in between. A non-modal panel is<br>not announced as a dialog, so never use it for a question that must be answered. |
| `trigger` | no | `ReactNode` |  |

### `ConfirmDialog`

Kind: callable.

```text
({ tone: toneProp, confirmTone, destructive, ...props }: ConfirmDialogProps) => import("react").JSX.Element
```

### `ConfirmDialogProps`

Kind: interface.

Extends: `OverlayBaseProps`, `OverlayActionProps`.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `tone` | no | `OverlayTone` |  |
| `showIcon` | no | `boolean` |  |
| `alertMessage` | no | `ReactNode` |  |
| `trigger` | no | `ReactNode` |  |
| `destructive` | no | `boolean` | Shorthand for the destructive presentation: tone, icon and confirm colour. |

### `defaultConfirmStrings`

Kind: const.

```text
OverlayActionStrings
```

### `defaultOverlayActionStrings`

Kind: const.

```text
OverlayActionStrings
```

### `OverlayActionProps`

Kind: interface.

The footer, and what confirm does. Exactly one confirm path runs, first match wins:

  1. `formId`: the button calls `requestSubmit()` on that form; neither callback fires.
  2. `onConfirm`: runs, then the overlay closes.
  3. `onAsyncConfirm`: awaited with a spinner; closes on success, stays open on
     rejection and reports through `onError`.
  4. none: the overlay just closes.

Passing `onConfirm` alongside `onAsyncConfirm` therefore skips the async one.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `showCancel` | no | `boolean` |  |
| `showConfirm` | no | `boolean` |  |
| `onCancel` | no | `() => void` |  |
| `onConfirm` | no | `() => void` |  |
| `onAsyncConfirm` | no | `() => Promise<void>` |  |
| `onError` | no | `(error: unknown) => void` | Receives a rejected async confirm, so a consumer can toast or report it. |
| `closeOnAsyncComplete` | no | `boolean` | `false` keeps the overlay open after a resolved async confirm (multi-step flows). |
| `confirmTone` | no | `OverlayButtonTone` |  |
| `confirmStyle` | no | `OverlayButtonStyle` |  |
| `loading` | no | `boolean` | Forces the pending presentation, for confirm work owned outside the overlay. |
| `formId` | no | `string` |  |
| `footer` | no | `ReactNode` | Replaces the generated footer. Every prop above stops applying. |

### `OverlayActionStrings`

Kind: interface.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `confirm` | yes | `string` |  |
| `cancel` | yes | `string` |  |

### `OverlayBaseProps`

Kind: interface.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `open` | no | `boolean` |  |
| `onOpenChange` | no | `(open: boolean) => void` |  |
| `onClose` | no | `() => void` | Fires after it closes, whatever dismissed it. |
| `children` | no | `ReactNode` |  |
| `title` | no | `ReactNode` | Supply one even with `hideHeader`: it names the dialog for screen readers. |
| `description` | no | `ReactNode` |  |
| `hideHeader` | no | `boolean` | Keeps the title and description for assistive technology and hides them. |
| `showCloseButton` | no | `boolean` |  |
| `initialFocusRef` | no | `RefObject<HTMLElement \| null>` |  |
| `closeOnEscape` | no | `boolean` | Escape dismisses. Off for destructive or in-progress work. |
| `closeOnBackdropClick` | no | `boolean` |  |
| `surfaceStyle` | no | `CSSProperties` | Inline styles on the surface itself, e.g. CSS variables a theme editor previews. |
| `className` | no | `string` |  |
| `contentClassName` | no | `string` |  |
| `strings` | no | `Partial<OverlayActionStrings>` |  |

### `OverlayButtonStyle`

Kind: type.

Ghost is excluded: a footer's confirm must look like a commitment.

```text
Exclude<ButtonStyle, "ghost">
```

### `OverlayButtonTone`

Kind: type.

```text
SemanticTone
```

### `OverlayEmphasisProps`

Kind: interface.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `emphasis` | no | `boolean` | Lets `tone` drive the header glyph and the confirm button's colour. Without it the<br>tone is presentational only (a warning notice with an ordinary primary confirm). |
| `tone` | no | `OverlayTone` |  |
| `showIcon` | no | `boolean` |  |
| `alertMessage` | no | `ReactNode` | A notice between the header and the body: the consequence, before committing. |

### `OverlayOpenSetter`

Kind: type.

```text
Dispatch<SetStateAction<boolean>>
```

### `OverlayTone`

Kind: type.

The tones an overlay can carry; a dialog is never "primary".

```text
Extract<SemanticTone, "neutral" | "destructive" | "warning" | "info" | "success">
```

### `OverlayVisibilityGroupState`

Kind: type.

```text
Record<TKey, boolean>
```

### `OverlayVisibilityProps`

Kind: interface.

Spreadable straight onto any overlay in the kit.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `open` | yes | `boolean` |  |
| `onOpenChange` | yes | `(open: boolean) => void` |  |

### `OverlayWidth`

Kind: type.

A named step or any CSS length.

```text
"sm" | "md" | "lg" | "xl" | "full" | (string & {})
```

### `useOverlayActions`

Kind: callable.

```text
({ close, open, onConfirm, onAsyncConfirm, closeOnAsyncComplete, confirmTone, loading, formId, onCancel, onError, strings, defaults, emphasis, tone, }: UseOverlayActionsOptions) => UseOverlayActionsReturn
```

### `UseOverlayActionsOptions`

Kind: interface.

Extends: `Pick<OverlayActionProps, "onConfirm" | "onAsyncConfirm" | "closeOnAsyncComplete" | "confirmTone" | "loading" | "formId" | "onCancel" | "onError">`.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `close` | yes | `() => void` |  |
| `open` | no | `boolean` | The surface session; closing invalidates an outstanding confirmation. |
| `strings` | no | `Partial<OverlayActionStrings>` |  |
| `defaults` | no | `OverlayActionStrings` | The defaults to start from (a confirmation says "Continue", not "Confirm"). |
| `emphasis` | no | `boolean` |  |
| `tone` | no | `OverlayTone` |  |

### `UseOverlayActionsReturn`

Kind: interface.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `copy` | yes | `OverlayActionStrings` |  |
| `pending` | yes | `boolean` | True while an async confirm is in flight. |
| `confirm` | yes | `() => void \| Promise<void>` |  |
| `cancel` | yes | `() => void` |  |
| `confirmTone` | yes | `OverlayButtonTone` |  |
| `busy` | yes | `boolean` | `pending`, or the caller's own `loading`. Both disable the footer. |

### `useOverlayVisibility`

Kind: callable.

```text
({ defaultOpen, open: controlledOpen, onOpenChange, onOpen, onClose, }?: UseOverlayVisibilityOptions) => UseOverlayVisibilityReturn
```

### `useOverlayVisibilityGroup`

Kind: callable.

Several overlays keyed by name from one hook, coordinated by `closeOthersOnOpen`.

```text
(keys: readonly TKey[], { defaultOpen, open: controlledOpen, onOpenChange, closeOthersOnOpen, }?: UseOverlayVisibilityGroupOptions<TKey>) => UseOverlayVisibilityGroupReturn<TKey>
```

### `UseOverlayVisibilityGroupOptions`

Kind: interface.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `defaultOpen` | no | `Partial<Record<TKey, boolean>>` |  |
| `open` | no | `Partial<Record<TKey, boolean>>` |  |
| `onOpenChange` | no | `(key: TKey, open: boolean, state: OverlayVisibilityGroupState<TKey>) => void` |  |
| `closeOthersOnOpen` | no | `boolean` | Closes the others when one opens, e.g. for dialogs driven from one menu. |

### `UseOverlayVisibilityGroupReturn`

Kind: type.

```text
Record<TKey, UseOverlayVisibilityReturn>
```

### `UseOverlayVisibilityOptions`

Kind: interface.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `defaultOpen` | no | `boolean` |  |
| `open` | no | `boolean` | Supplying this makes it controlled; the hook then only reports. |
| `onOpenChange` | no | `(open: boolean) => void` |  |
| `onOpen` | no | `() => void` | Fires only on the transitions, unlike `onOpenChange` which fires on both. |
| `onClose` | no | `() => void` |  |

### `UseOverlayVisibilityReturn`

Kind: interface.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `open` | yes | `boolean` |  |
| `setOpen` | yes | `OverlayOpenSetter` |  |
| `show` | yes | `() => void` |  |
| `hide` | yes | `() => void` |  |
| `toggle` | yes | `() => void` |  |
| `overlayProps` | yes | `OverlayVisibilityProps` | `{...visibility.overlayProps}` on the overlay. |

## Preview recipes

Preview route: Action overlays — `/action-overlays`

### ActionDialog

```tsx fragment — excerpt from the live preview; surrounding values are supplied by the application
<ActionDialog
  title="Rename workspace"
  trigger={<Button>Rename</Button>}
  onConfirm={save}
/>
```

### ConfirmDialog

```tsx fragment — excerpt from the live preview; surrounding values are supplied by the application
<ConfirmDialog
  destructive
  title="Delete this invoice?"
  description="This cannot be undone."
  trigger={<Button tone="destructive">Delete</Button>}
  onAsyncConfirm={remove}
/>
```

### ActionSheet

```tsx fragment — excerpt from the live preview; surrounding values are supplied by the application
<ActionSheet
  side="inline-end"
  title="Filters"
  modality="non-modal"
  trigger={<Button>Filter</Button>}
/>
```

### Driving them from elsewhere

```tsx fragment — excerpt from the live preview; surrounding values are supplied by the application
const overlays = useOverlayVisibilityGroup(["edit", "remove"], {
  closeOthersOnOpen: true,
})

<Button onClick={overlays.edit.show}>Edit</Button>
<ActionDialog {...overlays.edit.overlayProps} title="Edit" />
```
