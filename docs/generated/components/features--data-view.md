<!-- GENERATED by scripts/gen-consumer-docs.mjs — do not edit. -->

# `features/data-view`

One dataset is presented through switchable views with saved tabs, a toolbar and pagination.

## Selection

- **Use when:** One dataset is presented through switchable views with saved tabs, a toolbar and pagination.
- **Avoid when:** There is one table and no view switching, or you need the table body alone.
- **Consider instead:** `features/table`, `base/table`
- **Stability:** stable.

## Import

```tsx fragment — import-only example
import { DataView } from "themelia-ui/features/data-view"
import "themelia-ui/features/data-view.css"
```

## Optional peers

Install these only when importing this family:

```bash
npm install @tanstack/react-table
```

## Composition

This family composes `base/choice-inputs`, `base/feedback`, `base/navigation`, `base/structure`, `base/typography`, `features/filters`, `features/table`.

Application policy—routing, fetching, persistence, permissions, and translation—stays
outside the package and arrives through the public props, callbacks, slots, or accessors below.

## Public declarations

### `applyDataViewFilters`

Kind: callable.

Filters are ANDed; the values within one filter are ORed.

```text
(data: readonly TData[], activeFilters: readonly ActiveFilter[], filters: readonly FilterConfig[], getFilterValue?: DataViewFilterValueGetter<TData>) => readonly TData[]
```

### `DataView`

Kind: callable.

**Use when:** A resource browser combining a table, search, filters and saved views.

**Avoid when:** Only needing a table or replacing one small visual region with a new state machine.

**Compose:** Start with DataView props and slots; use DataViewShell or useDataView only for a genuinely different arrangement.

**Capabilities:** mobile filters for a table; saved views search sorting; resource list filtering.

**Alternatives:** DataTable, SearchFilters.

```text
({ data, columns, filtering, strings, table, slots, className, }: DataViewProps<TData, TValue>) => import("react").JSX.Element
```

### `DataViewFilteringConfig`

Kind: interface.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `filters` | yes | `FilterConfig[]` |  |
| `activeFilters` | yes | `ActiveFilter[]` |  |
| `onFilterChange` | yes | `(filters: ActiveFilter[]) => void` |  |
| `filterRows` | no | `DataViewFilterRows<TData>` |  |
| `getFilterValue` | no | `DataViewFilterValueGetter<TData>` |  |
| `tabs` | no | `FilterTab[]` |  |
| `tabsDisplay` | no | `"tabs" \| "select"` | A tab row, or a select — the same saved views, at two widths. |
| `tabsLabel` | no | `string` |  |
| `mobilePresentation` | no | `"sheet" \| "inline"` | Narrow viewports use the filters sheet by default. |
| `isFiltering` | no | `boolean` | A change is in flight; the bar dims. |
| `strings` | no | `Partial<FilterStrings>` |  |
| `onError` | no | `FilterErrorHandler` |  |

### `DataViewFilterRows`

Kind: type.

Replaces the built-in row matching entirely — for filtering done on the server.

```text
(args: DataViewFilterRowsArgs<TData>) => TData[]
```

### `DataViewFilterRowsArgs`

Kind: interface.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `data` | yes | `readonly TData[]` |  |
| `activeFilters` | yes | `readonly ActiveFilter[]` |  |
| `filters` | yes | `readonly FilterConfig[]` |  |

### `DataViewFilterValueGetter`

Kind: type.

Reads the value a filter compares against, for a nested or computed field.

```text
(row: TData, filter: FilterConfig) => unknown
```

### `DataViewPagination`

Kind: callable.

```text
({ page, pageCount, total, onPageChange, disabled, className, strings, }: DataViewPaginationProps) => import("react").JSX.Element | null
```

### `DataViewPaginationProps`

Kind: interface.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `page` | yes | `number` |  |
| `pageCount` | no | `number` |  |
| `total` | no | `ReactNode` | The result-count line beside the pager. |
| `onPageChange` | no | `(page: number) => void` |  |
| `disabled` | no | `boolean` |  |
| `className` | no | `string` |  |
| `strings` | no | `Partial<DataViewPaginationStrings>` |  |

### `DataViewPaginationStrings`

Kind: interface.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `label` | yes | `string` |  |
| `previous` | yes | `string` |  |
| `next` | yes | `string` |  |
| `page` | yes | `(page: number) => string` |  |

### `DataViewProps`

Kind: interface.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `data` | yes | `readonly TData[]` |  |
| `columns` | yes | `DataTableProps<TData, TValue>["columns"]` |  |
| `filtering` | no | `DataViewFilteringConfig<TData>` |  |
| `strings` | no | `Partial<DataViewStrings>` |  |
| `table` | no | `DataViewTableOptions<TData, TValue>` |  |
| `slots` | no | `DataViewSlots` |  |
| `className` | no | `string` |  |

### `DataViewShell`

Kind: callable.

```text
({ children, header, toolbar, footer, emptySlot, empty, className, contentClassName, }: DataViewShellProps) => import("react").JSX.Element
```

### `DataViewShellProps`

Kind: interface.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `children` | no | `ReactNode` |  |
| `header` | no | `ReactNode` |  |
| `toolbar` | no | `ReactNode` |  |
| `footer` | no | `ReactNode` |  |
| `emptySlot` | no | `ReactNode` | Rendered in place of the content when `empty` is set. |
| `empty` | no | `boolean` |  |
| `className` | no | `string` |  |
| `contentClassName` | no | `string` |  |

### `DataViewSlots`

Kind: interface.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `topbarContent` | no | `ReactNode` | Above the filter bar, inside the table's topbar. |
| `toolbarStart` | no | `ReactNode` | Leading content in the auxiliary row, beside the saved-view select. |
| `toolbarAfterFilters` | no | `ReactNode` |  |
| `topbarEnd` | no | `ReactNode` | Trailing content in the table's topbar, beside its own controls. |
| `footer` | no | `ReactNode` | Below the table, inside the frame. |

### `DataViewStrings`

Kind: interface.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `filterError` | yes | `string` |  |

### `DataViewTableFrame`

Kind: callable.

```text
({ toolbar, children, footer, className, toolbarClassName, contentClassName, footerClassName, }: DataViewTableFrameProps) => import("react").JSX.Element
```

### `DataViewTableFrameProps`

Kind: interface.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `toolbar` | no | `ReactNode` |  |
| `children` | no | `ReactNode` |  |
| `footer` | no | `ReactNode` |  |
| `className` | no | `string` |  |
| `toolbarClassName` | no | `string` |  |
| `contentClassName` | no | `string` |  |
| `footerClassName` | no | `string` |  |

### `DataViewTableOptions`

Kind: type.

The table's props, minus the ones DataView owns (e.g. `surface`: the frame draws the card).

```text
Omit<DataTableProps<TData, TValue>, "columns" | "data" | "surface" | "headerTransparent" | "topbarContent" | "topbarEnd">
```

### `DataViewToolbar`

Kind: callable.

```text
({ leading, search, filters, views, actions, className, }: DataViewToolbarProps) => import("react").JSX.Element
```

### `DataViewToolbarProps`

Kind: interface.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `leading` | no | `ReactNode` |  |
| `search` | no | `ReactNode` |  |
| `filters` | no | `ReactNode` |  |
| `views` | no | `ReactNode` |  |
| `actions` | no | `ReactNode` | Pushed to the trailing edge — the page's own actions, not the table's. |
| `className` | no | `string` |  |

### `defaultDataViewFilterTabsLabel`

Kind: const.

```text
unknown
```

### `defaultDataViewPaginationStrings`

Kind: const.

```text
DataViewPaginationStrings
```

### `defaultDataViewStrings`

Kind: const.

```text
DataViewStrings
```

### `SavedViewTab`

Kind: interface.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `id` | yes | `string` |  |
| `label` | yes | `ReactNode` |  |
| `count` | no | `ReactNode` |  |
| `disabled` | no | `boolean` |  |

### `SavedViewTabs`

Kind: callable.

A saved-view row that is NOT filter-driven — for a consumer holding views themselves.

```text
({ views, value, onValueChange, className }: SavedViewTabsProps) => import("react").JSX.Element
```

### `SavedViewTabsProps`

Kind: interface.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `views` | yes | `readonly SavedViewTab[]` |  |
| `value` | yes | `string` |  |
| `onValueChange` | yes | `(value: string) => void` |  |
| `className` | no | `string` |  |

### `useDataView`

Kind: callable.

```text
({ data, filtering, }: UseDataViewOptions<TData>) => UseDataViewResult<TData>
```

### `UseDataViewOptions`

Kind: interface.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `data` | yes | `readonly TData[]` |  |
| `filtering` | no | `DataViewFilteringConfig<TData>` |  |

### `UseDataViewResult`

Kind: interface.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `rows` | yes | `readonly TData[]` |  |
| `error` | yes | `unknown` |  |
| `failed` | yes | `boolean` | The matcher threw; `rows` is then the unfiltered data. |

## Preview recipes

Preview route: Data view & data table — `/data-view`

### An index

```tsx fragment — excerpt from the live preview; surrounding values are supplied by the application
// Filter and sort the complete collection before slicing a page.
// A server-backed view can pass the returned page and total instead.
const { rows } = useDataView({ data: bookings, filtering })
const sorted = useLegacyTable({
  data: rows, columns, state: { sorting },
  getCoreRowModel: getCoreRowModel(),
  getSortedRowModel: getSortedRowModel(),
})
const sortedRows = sorted.getRowModel().rows.map((row) => row.original)
const pageRows = sortedRows.slice((page - 1) * pageSize, page * pageSize)

<DataView
  data={pageRows}
  columns={columns}
  filtering={{
    ...filtering,
    filterRows: ({ data }) => data, // Already filtered before paging.
    tabs: savedViews,
  }}
  table={{ enableSorting: true, manualSorting: true, sorting, onSortingChange }}
  slots={{ footer: <DataViewPagination
    page={page}
    pageCount={Math.ceil(rows.length / pageSize)}
    total={resultSummary}
    onPageChange={setPage}
  /> }}
/>
```

### Pending results and recovery

```tsx fragment — excerpt from the live preview; surrounding values are supplied by the application
<DataView data={bookings} columns={columns} filtering={{
  filters, activeFilters, onFilterChange,
  isFiltering: pending,
  filterRows: matchRows,
  onError: reportError,
}} />
```
