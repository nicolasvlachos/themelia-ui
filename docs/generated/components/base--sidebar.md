<!-- GENERATED by scripts/gen-consumer-docs.mjs — do not edit. -->

# `base/sidebar`

Composing a collapsible sidebar from its parts, including the mobile presentation.

## Selection

- **Use when:** Composing a collapsible sidebar from its parts, including the mobile presentation.
- **Avoid when:** You want the assembled application sidebar with logo, user and workspace slots.
- **Consider instead:** `layout/sidebar`, `layout/workspace`
- **Stability:** stable.

## Import

```tsx fragment — import-only example
import { Sidebar } from "themelia-ui/base/sidebar"
import "themelia-ui/base/sidebar.css"
```

## Composition

This family composes `base/buttons`, `base/display`, `base/overlay`, `base/sheet`, `base/skeleton`, `base/text-inputs`, `base/tooltip`, `base/typography`.

Application policy—routing, fetching, persistence, permissions, and translation—stays
outside the package and arrives through the public props, callbacks, slots, or accessors below.

## Public declarations

### `defaultSidebarStrings`

Kind: const.

```text
SidebarStrings
```

### `Sidebar`

Kind: callable.

```text
({ side, variant, collapsible, className, surfaceClassName, mobileTitle, mobileDescription, strings, children, ...props }: SidebarProps) => React.JSX.Element
```

### `SidebarCollapsible`

Kind: type.

`icon` collapses to a glyph rail; `offcanvas` slides away; `none` never collapses.

```text
"offcanvas" | "icon" | "none"
```

### `SidebarContent`

Kind: callable.

```text
({ className, ...props }: ComponentProps<"div">) => React.JSX.Element
```

### `SidebarContextValue`

Kind: interface.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `state` | yes | `"expanded" \| "collapsed"` |  |
| `open` | yes | `boolean` |  |
| `setOpen` | yes | `(open: boolean \| ((open: boolean) => boolean)) => void` |  |
| `openMobile` | yes | `boolean` |  |
| `setOpenMobile` | yes | `(open: boolean) => void` |  |
| `isMobile` | yes | `boolean` |  |
| `toggleSidebar` | yes | `() => void` |  |
| `strings` | yes | `SidebarStrings` | Resolved copy, so every part of the sidebar reads the same override. |

### `SidebarFooter`

Kind: callable.

```text
({ className, ...props }: ComponentProps<"div">) => React.JSX.Element
```

### `SidebarGroup`

Kind: callable.

```text
({ className, ...props }: ComponentProps<"div">) => React.JSX.Element
```

### `SidebarGroupAction`

Kind: callable.

```text
({ className, ...props }: ComponentProps<"button">) => React.JSX.Element
```

### `SidebarGroupContent`

Kind: callable.

```text
({ className, ...props }: ComponentProps<"div">) => React.JSX.Element
```

### `SidebarGroupLabel`

Kind: callable.

```text
({ className, ...props }: ComponentProps<"div">) => React.JSX.Element
```

### `SidebarHeader`

Kind: callable.

```text
({ className, ...props }: ComponentProps<"div">) => React.JSX.Element
```

### `SidebarInput`

Kind: callable.

```text
({ className, ...props }: ComponentProps<typeof Input>) => React.JSX.Element
```

### `SidebarInset`

Kind: callable.

The page beside the panel. Renders `<main>`; a shell embedded in a host page that already
has one should pass `render={<div />}`.

```text
({ className, render, ...props }: useRender.ComponentProps<"main">) => React.ReactElement<unknown, string | React.JSXElementConstructor<any>>
```

### `SidebarMenu`

Kind: callable.

```text
({ className, ...props }: ComponentProps<"ul">) => React.JSX.Element
```

### `SidebarMenuAction`

Kind: callable.

```text
({ showOnHover, className, ...props }: SidebarMenuActionProps) => React.JSX.Element
```

### `SidebarMenuActionProps`

Kind: interface.

Extends: `ComponentProps<"button">`.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `showOnHover` | no | `boolean` | Reveals the action on hover or keyboard focus rather than showing it always. |

### `SidebarMenuBadge`

Kind: callable.

```text
({ inline, className, ...props }: SidebarMenuBadgeProps) => React.JSX.Element
```

### `SidebarMenuBadgeProps`

Kind: interface.

Extends: `ComponentProps<"div">`.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `inline` | no | `boolean` | Renders in normal flow instead of over the row — for a button laying out its own parts. |

### `SidebarMenuButton`

Kind: callable.

```text
({ size, active, variant, render, closeOnSelectMobile, tooltip, className, children, onClick, ...props }: SidebarMenuButtonProps) => React.JSX.Element
```

### `SidebarMenuButtonProps`

Kind: interface.

Extends: `Omit<ComponentProps<"button">, "size">`.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `size` | no | `SidebarMenuButtonSize` |  |
| `active` | no | `boolean` | Marks the row as the current page. |
| `variant` | no | `"default" \| "outline"` | `outline` gives the row its own frame — for a switcher that must read as a control. |
| `render` | no | `React.ReactElement` | The element this row becomes instead of a `button` — usually a router link<br>(docs/adr/0005); `children` stays the row's content. |
| `closeOnSelectMobile` | no | `boolean` | Dismisses the mobile sheet when the row is activated. Defaults to true; turn it off for<br>a row that opens something else (a switcher, a submenu toggle). |
| `tooltip` | no | `React.ReactNode` | Shown beside the row while the sidebar is collapsed to its icon rail, usually the row's<br>label. Ignored while expanded and on phones. |

### `SidebarMenuButtonSize`

Kind: type.

Row shape, not density: `lg` is an account or workspace switcher (avatar over two lines),
`sm` a secondary row.

```text
"sm" | "md" | "lg"
```

### `SidebarMenuItem`

Kind: callable.

```text
({ className, ...props }: ComponentProps<"li">) => React.JSX.Element
```

### `SidebarMenuSkeleton`

Kind: callable.

```text
({ showIcon, className, style, ...props }: SidebarMenuSkeletonProps) => React.JSX.Element
```

### `SidebarMenuSkeletonProps`

Kind: interface.

Extends: `ComponentProps<"div">`.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `showIcon` | no | `boolean` |  |

### `SidebarMenuSub`

Kind: callable.

```text
({ className, ...props }: ComponentProps<"ul">) => React.JSX.Element
```

### `SidebarMenuSubButton`

Kind: callable.

```text
({ size, active, closeOnSelectMobile, render, className, onClick, children, ...props }: SidebarMenuSubButtonProps) => React.JSX.Element
```

### `SidebarMenuSubButtonProps`

Kind: interface.

Extends: `ComponentProps<"a">`.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `size` | no | `"sm" \| "md"` |  |
| `active` | no | `boolean` |  |
| `closeOnSelectMobile` | no | `boolean` | Dismisses the mobile sheet when the row is activated. See SidebarMenuButton. |
| `render` | no | `React.ReactElement` | The element this row becomes instead of an `a` or `button` — usually a router link<br>(docs/adr/0005); `children` stays the row's content. |

### `SidebarMenuSubItem`

Kind: callable.

```text
({ className, ...props }: ComponentProps<"li">) => React.JSX.Element
```

### `SidebarProps`

Kind: interface.

Extends: `ComponentProps<"div">`.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `side` | no | `SidebarSide` |  |
| `variant` | no | `SidebarVariant` |  |
| `collapsible` | no | `SidebarCollapsible` |  |
| `surfaceClassName` | no | `string` | Class for the visible panel surface, separate from the positioning container. |
| `mobileTitle` | no | `string` | Accessible name for the mobile sheet. |
| `mobileDescription` | no | `string` |  |
| `strings` | no | `Partial<SidebarStrings>` | Overrides this sidebar's own copy, including the two above. |

### `SidebarProvider`

Kind: callable.

```text
({ defaultOpen, open: openProp, onOpenChange, persist, keyboardShortcut, contained, strings, className, style, children, ref, ...props }: SidebarProviderProps) => import("react").JSX.Element
```

### `SidebarProviderProps`

Kind: interface.

Extends: `Omit<React.ComponentProps<"div">, "onChange">`.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `strings` | no | `Partial<SidebarStrings>` | Overrides the sidebar's own copy for every part (it travels through the context). |
| `defaultOpen` | no | `boolean` | Initial expanded state when uncontrolled. |
| `open` | no | `boolean` |  |
| `onOpenChange` | no | `(open: boolean) => void` |  |
| `persist` | no | `boolean` | Remembers the expanded state across reloads, in `localStorage`. Defaults to true. |
| `keyboardShortcut` | no | `boolean` | Enables the ⌘B / Ctrl-B toggle shortcut. Defaults to true. |
| `contained` | no | `boolean` | Bounds the shell to this element instead of the viewport — for a shell that does not<br>own the screen (inside a tab, a preview, a nested editor). |

### `SidebarRail`

Kind: callable.

The strip on the panel's edge. Not a tab stop: it duplicates the trigger.

```text
({ className, ...props }: ComponentProps<"button">) => React.JSX.Element
```

### `SidebarSeparator`

Kind: callable.

```text
({ className, ...props }: ComponentProps<typeof Separator>) => React.JSX.Element
```

### `SidebarSide`

Kind: type.

Which edge the panel is docked to.

```text
"left" | "right"
```

### `SidebarStrings`

Kind: interface.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `toggle` | yes | `string` | Names the collapse control in both states — it is icon-only in both. |
| `mobileTitle` | yes | `string` | Title and description of the off-canvas sheet the rail becomes on a phone. |
| `mobileDescription` | yes | `string` |  |

### `SidebarTrigger`

Kind: callable.

The toggle. Pair it with the rail, which does the same job from the panel's edge.

```text
({ className, onClick, ...props }: ComponentProps<typeof Button>) => React.JSX.Element
```

### `SidebarVariant`

Kind: type.

`sidebar` sits against the shell edge with a border; `floating` is a rounded panel
inset from it; `inset` additionally lifts the page content into a rounded card.

```text
"sidebar" | "floating" | "inset"
```

### `useOptionalSidebar`

Kind: callable.

The context, or `null` outside a provider — for shell parts that render with or without
a sidebar (a header above a plain page).

```text
() => SidebarContextValue | null
```

### `useSidebar`

Kind: callable.

```text
() => SidebarContextValue
```

## Preview recipes

Preview route: App shell — `/app-shell`

### The shell

```tsx fragment — excerpt from the live preview; surrounding values are supplied by the application
<SidebarProvider>
  <Sidebar collapsible="icon">
    <SidebarHeader>…</SidebarHeader>
    <SidebarContent>
      <SidebarGroup>
        <SidebarGroupLabel>Workspace</SidebarGroupLabel>
        <SidebarMenu>
          <SidebarMenuItem>
            <SidebarMenuButton active>
              <FileTextIcon />
              <span>Invoices</span>
            </SidebarMenuButton>
            <SidebarMenuBadge>12</SidebarMenuBadge>
          </SidebarMenuItem>
        </SidebarMenu>
      </SidebarGroup>
    </SidebarContent>
    <SidebarRail />
  </Sidebar>
  <SidebarInset>{children}</SidebarInset>
</SidebarProvider>
```

### Anatomy

```tsx fragment — excerpt from the live preview; surrounding values are supplied by the application
<SidebarProvider>
  <Sidebar>
    <SidebarHeader>…</SidebarHeader>
    <SidebarContent>
      <SidebarGroup>
        <SidebarGroupLabel>Workspace</SidebarGroupLabel>
        <SidebarGroupContent>
          <SidebarMenu>
            <SidebarMenuItem>
              <SidebarMenuButton active>…</SidebarMenuButton>
              <SidebarMenuBadge>12</SidebarMenuBadge>
            </SidebarMenuItem>
          </SidebarMenu>
        </SidebarGroupContent>
      </SidebarGroup>
    </SidebarContent>
    <SidebarFooter>…</SidebarFooter>
    <SidebarRail />
  </Sidebar>
  <SidebarInset>…</SidebarInset>
</SidebarProvider>
```

### variant

```tsx fragment — excerpt from the live preview; surrounding values are supplied by the application
<Sidebar variant="floating" />
```

### collapsible

```tsx fragment — excerpt from the live preview; surrounding values are supplied by the application
<Sidebar collapsible="icon" />
```

### Controlled

```tsx fragment — excerpt from the live preview; surrounding values are supplied by the application
<SidebarProvider open={open} onOpenChange={setOpen}>…</SidebarProvider>
```
