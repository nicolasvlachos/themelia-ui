<!-- GENERATED by scripts/gen-consumer-docs.mjs — do not edit. -->

# `features/async-preview`

A trigger fetches its content when it opens, and needs loading, empty and error states.

## Selection

- **Use when:** A trigger fetches its content when it opens, and needs loading, empty and error states.
- **Avoid when:** The content is already loaded, or the surface is modal.
- **Consider instead:** `base/popover`, `features/overlays`
- **Stability:** stable.

## Import

```tsx fragment — import-only example
import { AsyncPreview } from "themelia-ui/features/async-preview"
import "themelia-ui/features/async-preview.css"
```

## Composition

This family composes `base/badge`, `base/feedback`, `base/popover`, `base/spinner`, `base/tooltip`.

Application policy—routing, fetching, persistence, permissions, and translation—stays
outside the package and arrives through the public props, callbacks, slots, or accessors below.

## Public declarations

### `AsyncPreview`

Kind: const.

```text
{ Root: typeof AsyncPreviewRoot; Trigger: typeof AsyncPreviewTrigger; Content: typeof AsyncPreviewContent; Body: typeof AsyncPreviewBody; State: typeof AsyncPreviewState; Loading: typeof AsyncPreviewLoading; Error: typeof AsyncPreviewError; Empty: typeof AsyncPreviewEmpty; }
```

### `AsyncPreviewBody`

Kind: callable.

The success case, with the data narrowed to non-null.

```text
({ children, }: AsyncPreviewBodyProps<TData, TContext, TType>) => import("react").JSX.Element | null
```

### `AsyncPreviewBodyProps`

Kind: interface.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `children` | yes | `(data: TData, state: AsyncPreviewState<TData, TContext, TType>) => ReactNode` | Rendered only in the `success` state, with the data non-null. |

### `AsyncPreviewCachePolicy`

Kind: type.

`cache-first` serves a fresh cache entry without a request. `always` refetches on every
open — for a value that changes while the reader is on the page.

```text
"cache-first" | "always"
```

### `AsyncPreviewContent`

Kind: callable.

```text
({ className, children, width, side, align, }: AsyncPreviewContentProps) => import("react").JSX.Element
```

### `AsyncPreviewContentProps`

Kind: interface.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `className` | no | `string` |  |
| `children` | no | `ReactNode` |  |
| `width` | no | `PopoverContentProps["width"]` | The popover's own vocabulary. Defaults to `"auto"` — a preview sizes to its record. |
| `side` | no | `PopoverContentProps["side"]` |  |
| `align` | no | `PopoverContentProps["align"]` |  |

### `AsyncPreviewDynamicRootProps`

Kind: interface.

A preview that fetches.

Extends: `AsyncPreviewRootBaseProps<TData, TContext, TType>`.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `onShow` | yes | `(args: AsyncPreviewShowArgs<TContext, TType>) => Promise<TData \| null \| undefined>` |  |
| `data` | no | `never` |  |

### `AsyncPreviewEmpty`

Kind: callable.

```text
({ className, children }: AsyncPreviewSlotProps) => import("react").JSX.Element | null
```

### `AsyncPreviewEmptyProps`

Kind: type.

```text
AsyncPreviewSlotProps<TData, TContext, TType>
```

### `AsyncPreviewError`

Kind: callable.

```text
({ className, children }: AsyncPreviewSlotProps) => import("react").JSX.Element | null
```

### `AsyncPreviewErrorProps`

Kind: type.

```text
AsyncPreviewSlotProps<TData, TContext, TType>
```

### `AsyncPreviewLoading`

Kind: callable.

```text
({ className, children }: AsyncPreviewSlotProps) => import("react").JSX.Element | null
```

### `AsyncPreviewLoadingProps`

Kind: type.

```text
AsyncPreviewSlotProps<TData, TContext, TType>
```

### `AsyncPreviewLoadingState`

Kind: type.

`false`, or a label to show beside the spinner while a slow step runs.

```text
false | { label?: ReactNode; }
```

### `AsyncPreviewReason`

Kind: type.

Why a fetch ran. `prefetch` fires on pointer-enter and is not repeated by the open that follows.

```text
"open" | "refresh" | "prefetch"
```

### `AsyncPreviewRoot`

Kind: callable.

```text
({ children, strings, ...options }: AsyncPreviewRootProps<TData, TContext, TType>) => import("react").JSX.Element
```

### `AsyncPreviewRootBaseProps`

Kind: interface.

Extends: `AsyncPreviewCallbacks<TData, TContext, TType>`.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `type` | yes | `TType` | Names the kind of record, passed to the fetcher. Caching requires cacheKey. |
| `context` | yes | `TContext` | Whatever the fetcher needs — usually an id. |
| `cacheKey` | no | `string` | The record identity and cache key. Change it when switching records; without one nothing is cached. |
| `cachePolicy` | no | `AsyncPreviewCachePolicy` |  |
| `staleTime` | no | `number` | How long a cache entry counts as fresh. Defaults to five minutes. |
| `open` | no | `boolean` |  |
| `defaultOpen` | no | `boolean` |  |
| `strings` | no | `Partial<AsyncPreviewStrings>` |  |
| `children` | no | `ReactNode` |  |

### `AsyncPreviewRootProps`

Kind: type.

```text
AsyncPreviewDynamicRootProps<TData, TContext, TType> | AsyncPreviewStaticRootProps<TData, TContext, TType>
```

### `AsyncPreviewShowArgs`

Kind: interface.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `type` | yes | `TType` |  |
| `context` | yes | `TContext` |  |
| `signal` | yes | `AbortSignal` | Aborted when the popover closes or a newer request starts. Pass it to fetch. |
| `reason` | yes | `AsyncPreviewReason` |  |
| `cached` | yes | `boolean` | Whether a cache entry existed. A revalidating fetch can render through it. |
| `setLoading` | yes | `(loading: AsyncPreviewLoadingState) => void` | Reports a slow step — "Decrypting…" — without resolving. |
| `setMeta` | yes | `(meta: Record<string, unknown>) => void` |  |

### `AsyncPreviewSlotProps`

Kind: interface.

The three placeholder slots. `children` may be a function of the state, e.g. to read the error.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `className` | no | `string` |  |
| `children` | no | `ReactNode \| ((state: AsyncPreviewState<TData, TContext, TType>) => ReactNode)` |  |

### `AsyncPreviewState`

Kind: callable.

The whole state, for a preview that renders its own four cases.

```text
({ children, }: AsyncPreviewStateProps<TData, TContext, TType>) => import("react").JSX.Element
```

### `AsyncPreviewStateProps`

Kind: interface.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `children` | yes | `(state: AsyncPreviewState<TData, TContext, TType>) => ReactNode` |  |

### `AsyncPreviewStateValue`

Kind: interface.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `type` | yes | `TType` |  |
| `context` | yes | `TContext` |  |
| `data` | yes | `TData \| null` |  |
| `error` | yes | `unknown` |  |
| `loading` | yes | `AsyncPreviewLoadingState` |  |
| `status` | yes | `AsyncPreviewStatus` |  |
| `meta` | yes | `Record<string, unknown>` | Anything the fetcher reported alongside the data — a count, a permission. |
| `open` | yes | `boolean` |  |
| `cached` | yes | `boolean` | Whether what is shown came from the cache rather than this open. |
| `refresh` | yes | `() => void` |  |
| `prefetch` | yes | `() => void` |  |
| `close` | yes | `() => void` |  |

### `AsyncPreviewStaticRootProps`

Kind: interface.

A preview whose data is already in hand: same component and states, no request.

Extends: `AsyncPreviewRootBaseProps<TData, TContext, TType>`.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `data` | yes | `TData \| null` |  |
| `onShow` | no | `never` |  |

### `AsyncPreviewStatus`

Kind: type.

`idle` before the first open; `empty` when the fetch resolved to nothing.

```text
"idle" | "loading" | "success" | "error" | "empty"
```

### `AsyncPreviewStrings`

Kind: interface.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `loading` | yes | `string` |  |
| `errorTitle` | yes | `string` |  |
| `errorDescription` | yes | `string` |  |
| `retry` | yes | `string` |  |
| `emptyTitle` | yes | `string` |  |
| `emptyDescription` | yes | `string` |  |

### `AsyncPreviewTrigger`

Kind: callable.

```text
({ children, prefetchOnHover, className, }: AsyncPreviewTriggerProps<TData, TContext, TType>) => import("react").JSX.Element
```

### `AsyncPreviewTriggerProps`

Kind: interface.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `children` | no | `ReactNode \| ((props: AsyncPreviewTriggerRenderProps<TData, TContext, TType>) => ReactElement)` | The element the preview hangs off. |
| `prefetchOnHover` | no | `boolean` | Starts the fetch on pointer-enter, before the popover opens. |
| `className` | no | `string` |  |

### `AsyncPreviewTriggerRenderProps`

Kind: interface.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `state` | yes | `AsyncPreviewState<TData, TContext, TType>` |  |

### `clearAsyncPreviewCache`

Kind: callable.

Clears one key, or everything. Call it after a mutation invalidates a record.

```text
(cacheKey?: string) => void
```

### `createAsyncPreview`

Kind: callable.

Binds the generics once, for a record shape used in more than one place.

```text
() => { Root: ({ children, strings, ...options }: AsyncPreviewRootProps<TData, TContext, TType>) => import("react").JSX.Element; Trigger: ({ children, prefetchOnHover, className, }: AsyncPreviewTriggerProps<TData, TContext, TType>) => import("react").JSX.Element; Content: typeof AsyncPreviewContent; Body: ({ children, }: AsyncPreviewBodyProps<TData, TContext, TType>) => import("react").JSX.Element | null; State: ({ children, }: AsyncPreviewStateProps<TData, TContext, TType>) => import("react").JSX.Element; Loading: typeof AsyncPreviewLoading; Error: typeof AsyncPreviewError; Empty: typeof AsyncPreviewEmpty; }
```

### `defaultAsyncPreviewStrings`

Kind: const.

```text
AsyncPreviewStrings
```

### `PreviewTriggerCell`

Kind: callable.

```text
({ value, secondary, leading, trailing, badge, icon: Icon, emptyLabel, hasPreview, disabledReason, asName, disabled, className, ...props }: PreviewTriggerCellProps) => import("react").JSX.Element
```

### `PreviewTriggerCellProps`

Kind: interface.

Extends: `Omit<ComponentProps<"button">, "children" | "value" | "name">`.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `value` | no | `ReactNode \| null` | The cell's own text. |
| `secondary` | no | `ReactNode \| null` | Second line, quieter. |
| `leading` | no | `ReactNode` |  |
| `trailing` | no | `ReactNode` |  |
| `badge` | no | `BadgeSpec` |  |
| `icon` | no | `ComponentType<{ className?: string; }>` |  |
| `emptyLabel` | no | `ReactNode` | Shown when `value` is absent — the em dash, unless a column wants its own word. |
| `hasPreview` | no | `boolean` | Whether this row has a preview at all. Off renders plain text. |
| `disabledReason` | no | `ReactNode` | Why it is unavailable. Shown as a tooltip on the inert cell. |
| `asName` | no | `boolean` | Runs the value through `Name` to normalise casing. |
| `className` | no | `string` |  |

### `useAsyncPreview`

Kind: callable.

```text
(options: UseAsyncPreviewOptions<TData, TContext, TType>) => UseAsyncPreviewReturn<TData, TContext, TType>
```

### `useAsyncPreviewContext`

Kind: callable.

```text
() => AsyncPreviewState<TData, TContext, TType>
```

### `UseAsyncPreviewOptions`

Kind: interface.

Extends: `Omit<AsyncPreviewRootProps<TData, TContext, TType>, "children" | "strings">`.

No own members are present in the normalized public snapshot.

### `UseAsyncPreviewReturn`

Kind: type.

```text
AsyncPreviewState<TData, TContext, TType> & { setOpen: (open: boolean) => void; }
```

## Preview recipes

Preview route: Async preview — `/async-preview`

### Fetching on open

```tsx fragment — excerpt from the live preview; surrounding values are supplied by the application
<AsyncPreview.Root
  type="customer"
  context={{ id }}
  cacheKey={`customer:${id}\
```

### PreviewTriggerCell

```tsx fragment — excerpt from the live preview; surrounding values are supplied by the application
<PreviewTriggerCell
  value={row.customerName}
  secondary={row.reference}
  hasPreview={row.customerId !== null}
  disabledReason="Imported without a customer record"
/>
```
