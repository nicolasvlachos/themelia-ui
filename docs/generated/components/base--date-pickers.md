<!-- GENERATED by scripts/gen-consumer-docs.mjs — do not edit. -->

# `base/date-pickers`

The user picks a date, range, month or time from a calendar surface.

## Selection

- **Use when:** The user picks a date, range, month or time from a calendar surface.
- **Avoid when:** The date is typed as text, or you are only displaying a date.
- **Consider instead:** `base/value-inputs`, `primitives`
- **Stability:** stable.

## Import

```tsx fragment — import-only example
import { Calendar } from "themelia-ui/base/date-pickers"
import "themelia-ui/base/date-pickers.css"
```

## Composition

This family composes `base/buttons`, `base/display`, `base/popover`, `base/text-inputs`, `base/typography`.

Application policy—routing, fetching, persistence, permissions, and translation—stays
outside the package and arrives through the public props, callbacks, slots, or accessors below.

## Public declarations

### `Calendar`

Kind: callable.

```text
({ strings, mode, value, onValueChange, month: monthProp, onMonthChange, numberOfMonths, weekStartsOn, className, ...constraints }: CalendarProps) => import("react").JSX.Element
```

### `CalendarConstraints`

Kind: interface.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `minDate` | no | `Date` | Earliest selectable day, inclusive. |
| `maxDate` | no | `Date` | Latest selectable day, inclusive. |
| `disabledDates` | no | `(date: Date) => boolean` | Refuses individual days — weekends, holidays, days already booked. |

### `CalendarProps`

Kind: interface.

Extends: `CalendarConstraints`.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `strings` | no | `Partial<CalendarStrings>` | Overrides this calendar's own copy — the three caption controls. |
| `mode` | no | `DateSelectionMode` |  |
| `value` | no | `Date \| Date[] \| DateRangeValue` | The selection, in the shape the mode uses. |
| `onValueChange` | no | `(value: Date \| Date[] \| DateRangeValue \| undefined) => void` |  |
| `month` | no | `Date` | The month shown. Uncontrolled unless supplied. |
| `onMonthChange` | no | `(month: Date) => void` |  |
| `numberOfMonths` | no | `number` | How many months to show side by side. Two is the usual range picker. |
| `weekStartsOn` | no | `0 \| 1 \| 2 \| 3 \| 4 \| 5 \| 6` | 0 is Sunday. Falls back to the provider's formatting config. |
| `className` | no | `string` |  |

### `CalendarStrings`

Kind: interface.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `previousMonth` | yes | `string` |  |
| `nextMonth` | yes | `string` |  |
| `chooseMonth` | yes | `string` | Describes what the caption button does (switch to the month grid); the month is its name. |
| `previousYear` | yes | `string` |  |
| `nextYear` | yes | `string` |  |

### `createRangePresets`

Kind: callable.

The usual reporting-range shortcuts, in the caller's language and on the caller's week.

```text
({ strings, weekStartsOn, now, }?: RangePresetOptions) => DatePreset[]
```

### `DatePicker`

Kind: callable.

```text
({ ref, id, mode, value, onValueChange, displayFormat, placeholder, disabled, invalid, clearable, strings, presets, numberOfMonths, closeOnSelect, header, footer, className, contentClassName, minDate, maxDate, disabledDates, ...aria }: DatePickerProps) => import("react").JSX.Element
```

### `DatePickerFooter`

Kind: callable.

```text
({ summary, actions, className, children, ...props }: DatePickerFooterProps) => import("react").JSX.Element | null
```

### `DatePickerFooterProps`

Kind: interface.

Extends: `ComponentProps<"div">`.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `summary` | no | `ReactNode` | A summary of what is chosen — "3 – 7 March, 5 days". |
| `actions` | no | `ReactNode` | Controls at the trailing edge. |

### `DatePickerHeader`

Kind: callable.

```text
({ title, description, action, className, children, ...props }: DatePickerHeaderProps) => import("react").JSX.Element | null
```

### `DatePickerHeaderProps`

Kind: interface.

Extends: `Omit<ComponentProps<"div">, "title">`.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `title` | no | `ReactNode` |  |
| `description` | no | `ReactNode` | A second line — the range's length, the constraint in force. |
| `action` | no | `ReactNode` | A control at the trailing edge — a mode switch, a clear. |

### `DatePickerProps`

Kind: interface.

Extends: `CalendarConstraints`.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `ref` | no | `Ref<HTMLButtonElement>` | The trigger button — the element a caller would focus or measure. |
| `id` | no | `string` | Applied to the trigger, the only labelable element. Its name comes from the chosen date,<br>so the field's label must reach it. |
| `mode` | no | `DateSelectionMode` |  |
| `value` | no | `Date \| Date[] \| DateRangeValue` |  |
| `onValueChange` | no | `(value: Date \| Date[] \| DateRangeValue \| undefined) => void` |  |
| `displayFormat` | no | `string` | How the chosen value is written in the trigger. |
| `placeholder` | no | `string` |  |
| `disabled` | no | `boolean` |  |
| `invalid` | no | `boolean` |  |
| `clearable` | no | `boolean` | Adds a clear control to the trigger once something is chosen. |
| `strings` | no | `Partial<DatePickerStrings>` | Overrides this picker's own copy — the clear control and the calendar's captions. |
| `presets` | no | `DatePreset[]` | Shortcuts down the side — "Last 30 days", "This month". |
| `numberOfMonths` | no | `number` |  |
| `closeOnSelect` | no | `boolean` | Closes the popup as soon as a complete value is chosen. |
| `header` | no | `ReactNode` | A band above the calendar — a title, a mode switch. Use `DatePickerHeader`. |
| `footer` | no | `ReactNode` | A band below it — a summary, Clear and Apply. Use `DatePickerFooter`. |
| `className` | no | `string` |  |
| `contentClassName` | no | `string` |  |
| `"aria-label"` | no | `string` |  |
| `"aria-labelledby"` | no | `string` |  |
| `"aria-describedby"` | no | `string` |  |

### `DatePickerStrings`

Kind: interface.

Extends: `CalendarStrings`.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `clear` | yes | `string` | Accessible name for the control that empties the field. |
| `placeholder` | yes | `string` | Shown in the trigger while there is no date. |
| `dateCount` | no | `(count: number) => string` | The trigger's text once more than two dates are chosen. Optional so older complete<br>strings objects still type-check. |

### `DatePreset`

Kind: interface.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `label` | yes | `string` |  |
| `value` | yes | `() => Date \| Date[] \| DateRangeValue` | Returns the value the preset selects, in whatever shape the mode uses. |

### `DateRangeValue`

Kind: interface.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `from` | no | `Date` |  |
| `to` | no | `Date` |  |

### `DateSelectionMode`

Kind: type.

```text
"single" | "multiple" | "range"
```

### `defaultCalendarStrings`

Kind: const.

```text
CalendarStrings
```

### `defaultDatePickerStrings`

Kind: const.

```text
DatePickerStrings
```

### `defaultRangePresetStrings`

Kind: const.

```text
RangePresetStrings
```

### `defaultTimePickerStrings`

Kind: const.

```text
TimePickerStrings
```

### `MonthYearPicker`

Kind: callable.

```text
({ value, onValueChange, minYear, maxYear, disabledMonths, strings, className, ...props }: MonthYearPickerProps) => import("react").JSX.Element
```

### `MonthYearPickerProps`

Kind: interface.

Extends: `Omit<ComponentProps<"div">, "onChange" | "value">`.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `value` | no | `MonthYearValue` |  |
| `onValueChange` | no | `(value: MonthYearValue) => void` |  |
| `minYear` | no | `number` |  |
| `maxYear` | no | `number` |  |
| `disabledMonths` | no | `(value: MonthYearValue) => boolean` | Refuses individual months — a period already closed, say. |
| `strings` | no | `Partial<CalendarStrings>` | Overrides this picker's own copy — the two year controls. |

### `MonthYearValue`

Kind: interface.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `month` | yes | `number` | 0-indexed, as `Date` months are — so it can be handed straight to `setMonth`. |
| `year` | yes | `number` |  |

### `MultipleDatePicker`

Kind: const.

```text
import('react').ForwardRefExoticComponent<Omit<MultipleDatePickerProps, "ref"> & import('react').RefAttributes<HTMLButtonElement>>
```

### `MultipleDatePickerProps`

Kind: interface.

Extends: `Base`.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `value` | no | `Date[]` |  |
| `onValueChange` | no | `(value: Date[]) => void` |  |

### `RangeDatePicker`

Kind: const.

```text
import('react').ForwardRefExoticComponent<Omit<RangeDatePickerProps, "ref"> & import('react').RefAttributes<HTMLButtonElement>>
```

### `RangeDatePickerProps`

Kind: interface.

Extends: `Base`.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `value` | no | `DateRangeValue` |  |
| `onValueChange` | no | `(value: DateRangeValue) => void` |  |

### `RangePresetOptions`

Kind: interface.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `strings` | no | `Partial<RangePresetStrings>` | Overrides the seven preset labels. |
| `weekStartsOn` | no | `0 \| 1 \| 2 \| 3 \| 4 \| 5 \| 6` | Which day a week starts on; pass the provider's `dates.weekStartsOn`. Unset falls back to<br>date-fns (Sunday), which disagrees with the provider's Monday default. |
| `now` | no | `() => Date` | What "now" is, injectable for tests. A function so presets stay correct on a page left<br>open past midnight. |

### `RangePresetStrings`

Kind: interface.

The seven range shortcuts; separate from `DatePickerStrings` because presets are the caller's list.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `last7Days` | yes | `string` |  |
| `last30Days` | yes | `string` |  |
| `thisWeek` | yes | `string` |  |
| `thisMonth` | yes | `string` |  |
| `lastMonth` | yes | `string` |  |
| `thisYear` | yes | `string` |  |
| `lastYear` | yes | `string` |  |

### `SingleDatePicker`

Kind: const.

```text
import('react').ForwardRefExoticComponent<Omit<SingleDatePickerProps, "ref"> & import('react').RefAttributes<HTMLButtonElement>>
```

### `SingleDatePickerProps`

Kind: interface.

Extends: `Base`.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `value` | no | `Date` |  |
| `onValueChange` | no | `(value: Date \| undefined) => void` |  |

### `TimePicker`

Kind: callable.

```text
({ id, value, onValueChange, withSeconds, minuteStep, disabled, invalid, className, strings, "aria-labelledby": fieldLabelledBy, "aria-describedby": describedBy, "aria-invalid": ariaInvalid, "aria-required": ariaRequired, }: TimePickerProps) => import("react").JSX.Element
```

### `TimePickerProps`

Kind: interface.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `id` | no | `string` | Applied to the hours segment, the one a `FormField` label addresses; each segment keeps<br>its own part name. |
| `value` | no | `TimeValue` |  |
| `onValueChange` | no | `(value: TimeValue) => void` |  |
| `withSeconds` | no | `boolean` | Adds a seconds segment. |
| `minuteStep` | no | `number` | Minutes snap to this. 15 gives a quarter-hour picker. |
| `disabled` | no | `boolean` |  |
| `invalid` | no | `boolean` |  |
| `className` | no | `string` |  |
| `strings` | no | `Partial<TimePickerStrings>` | Overrides this picker's own copy — the group and the three segments. |
| `"aria-labelledby"` | no | `string` |  |
| `"aria-describedby"` | no | `string` |  |
| `"aria-invalid"` | no | `boolean \| "true" \| "false"` |  |
| `"aria-required"` | no | `boolean \| "true" \| "false"` |  |

### `TimePickerStrings`

Kind: interface.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `label` | yes | `string` | Names the group of segments. The three boxes are one value. |
| `hours` | yes | `string` | Each segment. They are unlabelled boxes, so this is the only name they have. |
| `minutes` | yes | `string` |  |
| `seconds` | yes | `string` |  |

### `TimeValue`

Kind: interface.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `hours` | yes | `number` |  |
| `minutes` | yes | `number` |  |
| `seconds` | no | `number` |  |

## Preview recipes

Preview route: Calendar — `/calendar`

### Calendar

```tsx fragment — excerpt from the live preview; surrounding values are supplied by the application
<Calendar mode="range" value={range} onValueChange={setRange} numberOfMonths={2} />
```

### Another language, another week

```tsx fragment — excerpt from the live preview; surrounding values are supplied by the application
import { de } from "date-fns/locale"

<UIProvider config={{ dates: { locale: de, weekStartsOn: 0 } }}>
  <Calendar mode="single" />
</UIProvider>
```

### DatePicker

```tsx fragment — excerpt from the live preview; surrounding values are supplied by the application
<DatePicker value={day} onValueChange={setDay} clearable />
const { weekStartsOn } = useDatesConfig()
const presets = useMemo(() => createRangePresets({ weekStartsOn }), [weekStartsOn])
<DatePicker mode="range" value={range} onValueChange={setRange} presets={presets} />
```

### One picker, four fixed modes

```tsx fragment — excerpt from the live preview; surrounding values are supplied by the application
<SingleDatePicker value={day} onValueChange={setDay} />
<RangeDatePicker value={range} onValueChange={setRange} />
<MonthYearPicker value={month} onValueChange={setMonth} />
```

### TimePicker and DateTimeInput

```tsx fragment — excerpt from the live preview; surrounding values are supplied by the application
<TimePicker value={time} onValueChange={setTime} minuteStep={15} />
<DateTimeInput value={iso} onValueChange={setIso} />
```
