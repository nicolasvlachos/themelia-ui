<!-- GENERATED by scripts/gen-consumer-docs.mjs — do not edit. -->

# `base/skeleton`

A placeholder should hold the eventual layout's shape while data loads.

## Selection

- **Use when:** A placeholder should hold the eventual layout's shape while data loads.
- **Avoid when:** The wait is indeterminate and unstructured, or the region failed.
- **Consider instead:** `base/spinner`, `base/feedback`
- **Stability:** stable.

## Import

```tsx fragment — import-only example
import { ContentSkeleton } from "themelia-ui/base/skeleton"
import "themelia-ui/base/skeleton.css"
```

## Composition

This family composes `base/display`.

Application policy—routing, fetching, persistence, permissions, and translation—stays
outside the package and arrives through the public props, callbacks, slots, or accessors below.

## Public declarations

### `ContentSkeleton`

Kind: callable.

```text
({ lines, showTitle, label, strings, className, ...props }: ContentSkeletonProps) => import("react").JSX.Element
```

### `ContentSkeletonProps`

Kind: interface.

Extends: `ComponentProps<"div">`.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `lines` | no | `number` | Number of text lines. The last is short, so the block reads as prose. |
| `showTitle` | no | `boolean` | A heading above the lines. |
| `label` | no | `string` | Announced in place of the shapes. |
| `strings` | no | `Partial<SkeletonStrings>` | Overrides the default announcement. `label` still wins for one instance. |

### `defaultSkeletonStrings`

Kind: const.

```text
SkeletonStrings
```

### `PageSkeleton`

Kind: callable.

```text
({ showHeader, blocks, label, strings, className, ...props }: PageSkeletonProps) => import("react").JSX.Element
```

### `PageSkeletonProps`

Kind: interface.

Extends: `ComponentProps<"div">`.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `showHeader` | no | `boolean` | The page header: a title, its description, and the actions at the end of the row. |
| `blocks` | no | `number` | Content panels below the header. |
| `label` | no | `string` |  |
| `strings` | no | `Partial<SkeletonStrings>` | Overrides the default announcement. `label` still wins for one instance. |

### `Skeleton`

Kind: callable.

```text
({ className, ...props }: React.ComponentProps<"div">) => React.JSX.Element
```

### `SkeletonStrings`

Kind: interface.

What a skeleton announces while there is nothing to read. `label` overrides per instance.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `loading` | yes | `string` | A region of content. |
| `loadingPage` | yes | `string` | A whole page's worth. |
| `loadingTable` | yes | `string` | A table, where the shape being loaded is worth naming. |

### `TableSkeleton`

Kind: callable.

```text
({ rows, columns, showHeader, framed, label, strings, className, ...props }: TableSkeletonProps) => import("react").JSX.Element
```

### `TableSkeletonProps`

Kind: interface.

Extends: `ComponentProps<"div">`.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `rows` | no | `number` |  |
| `columns` | no | `number` | Match the real table, or the placeholder reflows when the data lands. |
| `showHeader` | no | `boolean` |  |
| `framed` | no | `boolean` | Draws the table's own edge, as DataView does. Turn off inside a card that has one. |
| `label` | no | `string` |  |
| `strings` | no | `Partial<SkeletonStrings>` | Overrides the default announcement. `label` still wins for one instance. |

### `TwoColumnPageSkeleton`

Kind: callable.

A detail page: a title over the record's panel, beside its owner and facts.

```text
({ label, strings, className, ...props }: TwoColumnPageSkeletonProps) => import("react").JSX.Element
```

### `TwoColumnPageSkeletonProps`

Kind: interface.

Extends: `ComponentProps<"div">`.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `label` | no | `string` |  |
| `strings` | no | `Partial<SkeletonStrings>` | Overrides the default announcement. `label` still wins for one instance. |

## Preview recipes

Preview route: Skeleton — `/skeleton`

### Skeleton

```tsx fragment — excerpt from the live preview; surrounding values are supplied by the application
<Skeleton style={{ width: "12rem", height: "1rem" }} />
```

### Composed skeletons

```tsx fragment — excerpt from the live preview; surrounding values are supplied by the application
<ContentSkeleton lines={3} />
<TableSkeleton rows={4} columns={3} />
<PageSkeleton blocks={2} />
<TwoColumnPageSkeleton />
```
