<!-- GENERATED by scripts/gen-consumer-docs.mjs — do not edit. -->

# `base/spinner`

A wait is short and has no layout to preview.

## Selection

- **Use when:** A wait is short and has no layout to preview.
- **Avoid when:** The eventual shape is known, or the wait blocks a whole region.
- **Consider instead:** `base/skeleton`, `base/feedback`
- **Stability:** stable.

## Import

```tsx fragment — import-only example
import { Spinner } from "themelia-ui/base/spinner"
import "themelia-ui/base/spinner.css"
```

## Composition

This family has no component-family dependencies.

Application policy—routing, fetching, persistence, permissions, and translation—stays
outside the package and arrives through the public props, callbacks, slots, or accessors below.

## Public declarations

### `Spinner`

Kind: callable.

```text
({ size, tone, label, className, ...props }: SpinnerProps) => React.JSX.Element
```

### `SpinnerProps`

Kind: interface.

Extends: `Omit<React.ComponentProps<"span">, "children">`.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `size` | no | `ComponentScale` |  |
| `tone` | no | `SemanticTone` |  |
| `label` | no | `React.ReactNode` | Visible label beside the ring. Also becomes the announced status. |

## Preview recipes

Preview route: Spinner — `/spinner`

### Spinner

```tsx fragment — excerpt from the live preview; surrounding values are supplied by the application
<Spinner label="Saving…" />   {/* announced */}
<Spinner />                   {/* decorative, aria-hidden */}
```

### Size

```tsx fragment — excerpt from the live preview; surrounding values are supplied by the application
<Spinner size="sm" />
<Spinner size="md" />
<Spinner size="lg" />
```

### Tone

```tsx fragment — excerpt from the live preview; surrounding values are supplied by the application
<Spinner tone="neutral" />
<Spinner tone="success" />
```
