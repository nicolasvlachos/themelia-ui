export interface NavigationTabsStrings {
	/** Names the navigation region. A page with two tab rows needs two names. */
	label: string
}

export const defaultNavigationTabsStrings: NavigationTabsStrings = {
	label: "Sections",
}

export interface OverflowTabBarStrings {
	label: string
}

export const defaultOverflowTabBarStrings: OverflowTabBarStrings = {
	label: "Sections",
}
