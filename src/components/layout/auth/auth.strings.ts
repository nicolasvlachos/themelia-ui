export interface AuthShellStrings {
	/** Names for the three link rows below the card; each row is its own named `<nav>`. */
	languageLabel: string
	helpLabel: string
	legalLabel: string
}

export const defaultAuthShellStrings: AuthShellStrings = {
	languageLabel: "Language",
	helpLabel: "Help",
	legalLabel: "Legal",
}
