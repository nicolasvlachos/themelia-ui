<!-- GENERATED by scripts/gen-consumer-docs.mjs — do not edit. -->

# `base/badge`

A short status or count sits beside the thing it describes and carries a semantic tone.

## Selection

- **Use when:** A short status or count sits beside the thing it describes and carries a semantic tone.
- **Avoid when:** The text is a full sentence, or the tone is decoration rather than state.
- **Consider instead:** `base/display`, `base/typography`
- **Stability:** stable.

## Import

```tsx fragment — import-only example
import { Badge } from "themelia-ui/base/badge"
import "themelia-ui/base/badge.css"
```

## Composition

This family composes `base/slot`.

Application policy—routing, fetching, persistence, permissions, and translation—stays
outside the package and arrives through the public props, callbacks, slots, or accessors below.

## Public declarations

### `Badge`

Kind: callable.

```text
({ className, tone, variant, render, dot, pending, pulse, children, ...props }: BadgeProps) => React.JSX.Element
```

### `BadgeProps`

Kind: interface.

Extends: `React.ComponentProps<"span">`, `VariantProps<typeof badgeVariants>`.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `render` | no | `React.ReactElement` | The element this becomes — an anchor, a router link, a label (docs/adr/0005).<br>`children` stays the content. |
| `dot` | no | `boolean` | A leading status dot in the badge's own tone. |
| `pending` | no | `boolean` | Draws the dot hollow, for a state that hasn't happened yet ("queued", not "failed"). |
| `pulse` | no | `boolean` | Animates the dot, for a state that is actively changing. |

### `BadgeTone`

Kind: type.

```text
"neutral" | "primary" | "secondary" | "success" | "info" | "warning" | "destructive"
```

### `BadgeVariant`

Kind: type.

Structural presentation. The tone supplies the hue; this decides how it is applied.

```text
"soft" | "solid" | "outline"
```

### `badgeVariants`

Kind: callable.

```text
(props?: ({ tone?: ("neutral" | "primary" | "secondary" | "info" | "success" | "warning" | "destructive") | null | undefined; variant?: ("solid" | "outline" | "soft") | null | undefined; } & { className?: import('clsx').ClassValue; }) | undefined) => string
```

## Preview recipes

Preview route: Badge — `/badge`

### Tones

```tsx fragment — excerpt from the live preview; surrounding values are supplied by the application
<Badge tone="success">Paid</Badge>
<Badge tone="warning" variant="solid">Due</Badge>
<Badge tone="destructive" variant="outline">Overdue</Badge>
```

### Status dot

```tsx fragment — excerpt from the live preview; surrounding values are supplied by the application
<Badge tone="success" dot>Live</Badge>
<Badge tone="warning" dot pending>Queued</Badge>
<Badge tone="info" dot pulse>Syncing</Badge>
```
