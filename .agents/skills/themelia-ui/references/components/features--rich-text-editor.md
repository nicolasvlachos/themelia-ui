<!-- GENERATED by scripts/gen-consumer-docs.mjs — do not edit. -->

# `features/rich-text-editor`

Users author formatted content with a toolbar and counts.

## Selection

- **Use when:** Users author formatted content with a toolbar and counts.
- **Avoid when:** The input is plain multiline text, or the content is only displayed.
- **Consider instead:** `base/text-inputs`, `base/typography`
- **Stability:** stable.

## Import

```tsx fragment — import-only example
import { EditorCounts } from "themelia-ui/features/rich-text-editor"
import "themelia-ui/features/rich-text-editor.css"
```

## Optional peers

Install these only when importing this family:

```bash
npm install @tiptap/core @tiptap/pm @tiptap/starter-kit
```

## Composition

This family composes `base/text-inputs`, `base/toolbar`, `base/typography`, `features/rich-text-editor/tiptap`.

Application policy—routing, fetching, persistence, permissions, and translation—stays
outside the package and arrives through the public props, callbacks, slots, or accessors below.

## Public declarations

### `createExecCommandEngine`

Kind: callable.

```text
(element: HTMLElement) => RichTextEngine
```

### `defaultRichTextEditorStrings`

Kind: const.

```text
RichTextEditorStrings
```

### `EditorCounts`

Kind: callable.

Character and word counts. Over the limit the count turns error-toned; input is never
refused, so the form decides.

```text
({ text, maxLength, strings, className }: EditorCountsProps) => import("react").JSX.Element
```

### `EditorCountsProps`

Kind: interface.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `text` | yes | `string` | The document as plain text. |
| `maxLength` | no | `number` |  |
| `strings` | yes | `RichTextEditorStrings` |  |
| `className` | no | `string` |  |

### `htmlToText`

Kind: callable.

A plain-text projection for counting. Block ends become a space (so paragraphs do not
merge words); inline tags collapse to nothing. Not a sanitiser: the output never reaches
the DOM.

```text
(html: string) => string
```

### `insertHtmlAtCaret`

Kind: callable.

Inserts at the caret, or at the end when the caret is somewhere else entirely.

```text
(editable: HTMLElement | null, html: string) => void
```

### `normalizeHtml`

Kind: callable.

`""` for an empty document, whatever shape the browser left it in; the placeholder,
submit state and `isEmpty()` all depend on it.

```text
(value: string) => string
```

### `readCaretContext`

Kind: callable.

Reads the text between the start of the caret's block and the caret. Stops at the block
boundary: a trigger is typed within a sentence.

```text
() => RichTextCaretContext | null
```

### `replaceBeforeCaret`

Kind: callable.

Replaces the `length` characters before the caret with `html` in one operation: a range
over the characters is inserted over, never deleted first. Counts back through text
nodes, since the run may span several after a browser split.

```text
(length: number, html: string, onChange: () => void) => void
```

### `RichTextCaretContext`

Kind: interface.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `textBefore` | yes | `string` | Text before the caret, after the last atomic inline node or line break in this block. |
| `rect` | no | `{ top: number; left: number; bottom: number; right: number; }` | Where the caret is on screen, when the browser will say. |

### `RichTextCommand`

Kind: type.

```text
"bold" | "italic" | "underline" | "strike" | "bulletList" | "orderedList" | "blockquote" | "undo" | "redo"
```

### `RichTextEditor`

Kind: callable.

```text
({ engine, value, onValueChange, placeholder, compact, minHeight, maxHeight, disabled, showCounts, maxLength, extraToolbarItems, toolbarTrailing, footerSlot, hideSourceToggle, autoFocus, className, onCaretChange, strings, ref, }: RichTextEditorProps & { ref?: Ref<RichTextEditorHandle>; }) => import("react").JSX.Element
```

### `RichTextEditorHandle`

Kind: interface.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `focus` | yes | `void` |  |
| `getHTML` | yes | `string` |  |
| `setHTML` | yes | `void` |  |
| `insertHTML` | yes | `void` | Inserts raw HTML at the caret. |
| `isEmpty` | yes | `boolean` |  |
| `clear` | yes | `void` |  |
| `getCaretContext` | yes | `RichTextCaretContext \| null` |  |
| `replaceBeforeCaret` | yes | `void` | Deletes the `length` characters before the caret and inserts `html` in their place,<br>as one operation (one undo entry, no intermediate caret state). |

### `RichTextEditorProps`

Kind: interface.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `engine` | no | `RichTextEngine` | What edits the document. Defaults to TipTap with StarterKit and inline mentions; the<br>chrome (toolbar, source view, counts, footer, strings) is engine-independent. Install<br>the family's documented TipTap peers when importing the editor. |
| `value` | yes | `string` | The document, as HTML. Controlled. |
| `onValueChange` | yes | `(html: string) => void` |  |
| `placeholder` | no | `string` | Drawn over the empty document. |
| `compact` | no | `boolean` | Tightens the toolbar and shortens the body, for a composer rather than a page. |
| `minHeight` | no | `string` |  |
| `maxHeight` | no | `string` | Where the body starts scrolling instead of growing. |
| `disabled` | no | `boolean` |  |
| `showCounts` | no | `boolean` |  |
| `maxLength` | no | `number` | Shown as "n / max". Does not truncate: the count reports, the form decides. |
| `extraToolbarItems` | no | `ReadonlyArray<RichTextEditorToolbarItem>` |  |
| `toolbarTrailing` | no | `ReactNode` | Pinned to the end of the toolbar, usually the submit control. |
| `footerSlot` | no | `ReactNode` | Below the body: attachment chips, a hint. |
| `hideSourceToggle` | no | `boolean` |  |
| `autoFocus` | no | `boolean` |  |
| `className` | no | `string` |  |
| `onCaretChange` | no | `() => void` | Fires after every input and every selection change (the caret can move without the<br>document changing); read `getCaretContext()` from the handle in response. |
| `strings` | no | `Partial<RichTextEditorStrings>` |  |

### `RichTextEditorStrings`

Kind: interface.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `editorLabel` | yes | `string` | Names the editable surface for a screen reader. |
| `sourceLabel` | yes | `string` | Names the HTML source textarea. |
| `toolbarLabel` | no | `string` | Names the formatting toolbar as a whole. |
| `toolbar` | yes | `{ bold: string; italic: string; strike: string; bulletList: string; orderedList: string; blockquote: string; undo: string; redo: string; sourceCode: string; }` | Toolbar labels. Each is both the accessible name and the tooltip. |
| `counts` | yes | `{ characters: string; words: string; }` |  |

### `RichTextEditorToolbar`

Kind: callable.

```text
({ buttons, extraToolbarItems, hideSourceToggle, sourceMode, toggleSourceMode, disabled, toolbarTrailing, strings, className, }: RichTextEditorToolbarProps) => import("react").JSX.Element
```

### `RichTextEditorToolbarItem`

Kind: interface.

A control the consumer adds to the toolbar, after the built-ins.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `id` | yes | `string` |  |
| `icon` | yes | `ComponentType<{ className?: string; }>` |  |
| `label` | yes | `string` |  |
| `onClick` | yes | `() => void` |  |
| `isActive` | no | `() => boolean` |  |
| `disabled` | no | `boolean` |  |

### `RichTextEditorToolbarProps`

Kind: interface.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `buttons` | yes | `ToolbarButtonConfig[]` |  |
| `extraToolbarItems` | no | `ReadonlyArray<RichTextEditorToolbarItem>` |  |
| `hideSourceToggle` | no | `boolean` |  |
| `sourceMode` | yes | `boolean` |  |
| `toggleSourceMode` | yes | `() => void` |  |
| `disabled` | no | `boolean` |  |
| `toolbarTrailing` | no | `ReactNode` |  |
| `strings` | yes | `RichTextEditorStrings` |  |
| `className` | no | `string` |  |

### `RichTextEngine`

Kind: interface.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `mount` | no | `HTMLElement` | Attaches an engine-owned view and returns its actual editable element. |
| `unmount` | no | `void` | Detaches the view while retaining the document, selection, and undo history. |
| `setEditable` | no | `void` | Updates whether the engine-owned view accepts user edits. |
| `getState` | yes | `RichTextEngineState` | The current state, returning the same object until something changes: the shell reads<br>it through `useSyncExternalStore`, and a fresh object per call re-renders forever. |
| `setHtml` | yes | `void` |  |
| `insertHtml` | no | `void` | Inserts HTML at the engine's selection through its document model. |
| `getCaretContext` | no | `RichTextCaretContext \| null` | Reads a focused, collapsed caret after the last atom or line break in its text block. |
| `replaceBeforeCaret` | no | `void` | Replaces text before a collapsed caret without crossing its text block or inline atoms. |
| `focus` | yes | `void` |  |
| `execute` | yes | `void` |  |
| `subscribe` | yes | `() => void` | Returns an unsubscribe. The shell reads through `useSyncExternalStore`. |
| `destroy` | yes | `void` | Idempotent, including when already unmounted. |

### `RichTextEngineState`

Kind: interface.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `html` | yes | `string` | The document as HTML; what the consumer's `onValueChange` receives. |
| `canUndo` | yes | `boolean` |  |
| `canRedo` | yes | `boolean` |  |
| `active` | yes | `ReadonlySet<RichTextCommand>` | Which commands apply at the caret, for the toolbar's pressed state. |

### `toEditorContent`

Kind: callable.

The inverse: seeds an empty paragraph so the first block command has something to act on.

```text
(value: string) => string
```

### `ToolbarButtonConfig`

Kind: interface.

The shape the toolbar renders. Built-ins and consumer items resolve to this.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `id` | yes | `string` |  |
| `icon` | yes | `ComponentType<{ className?: string; }>` |  |
| `label` | yes | `string` |  |
| `isActive` | yes | `() => boolean` |  |
| `run` | yes | `() => void` |  |
| `disabled` | no | `boolean` |  |

## Preview recipes

Preview route: Rich text editor — `/rich-text-editor`

### The editor

```tsx fragment — excerpt from the live preview; surrounding values are supplied by the application
<RichTextEditor
  value={body}
  onValueChange={setBody}
  showCounts
  maxLength={280}
  placeholder="Write something…"
/>
```

### compact

```tsx fragment — excerpt from the live preview; surrounding values are supplied by the application
<RichTextEditor
  compact
  value={note}
  onValueChange={setNote}
  hideSourceToggle
  footerSlot={<><AttachmentChips /><Button>Post</Button></>}
/>
```

### Custom engines

```tsx fragment — excerpt from the live preview; surrounding values are supplied by the application
// Default: StarterKit and atomic mentions.
<RichTextEditor value={html} onValueChange={setHtml} />

// Custom schema: create after mount, outside React render.
import { useEffect, useState } from "react"
import StarterKit from "@tiptap/starter-kit"
import { RichTextEditor, type RichTextEngine } from "themelia-ui/features/rich-text-editor"
import { createTiptapEngine } from "themelia-ui/features/rich-text-editor/tiptap"

function CustomEditor() {
  const [html, setHtml] = useState("")
  const [engine, setEngine] = useState<RichTextEngine>()
  useEffect(() => {
    const next = createTiptapEngine({
      element: document.createElement("div"),
      extensions: [StarterKit.configure({ heading: false, trailingNode: false })],
    })
    setEngine(next)
    return () => next.destroy()
  }, [])
  return engine ? <RichTextEditor engine={engine} value={html} onValueChange={setHtml} /> : null
}
```
