<!-- GENERATED by scripts/gen-consumer-docs.mjs — do not edit. -->

# `base/timeline`

Composing a generic vertical sequence of dated entries.

## Selection

- **Use when:** Composing a generic vertical sequence of dated entries.
- **Avoid when:** The sequence is a changelog, milestones or a step tracker.
- **Consider instead:** `patterns/timelines`, `features/activities`
- **Stability:** stable.

## Import

```tsx fragment — import-only example
import { Stepper } from "themelia-ui/base/timeline"
import "themelia-ui/base/timeline.css"
```

## Composition

This family composes `base/display`, `base/typography`.

Application policy—routing, fetching, persistence, permissions, and translation—stays
outside the package and arrives through the public props, callbacks, slots, or accessors below.

## Public declarations

### `defaultStepperStrings`

Kind: const.

```text
StepperStrings
```

### `Stepper`

Kind: callable.

```text
({ steps, variant, onStepClick, strings, className, ...props }: StepperProps) => import("react").JSX.Element
```

### `StepperProps`

Kind: interface.

Extends: `Omit<ComponentProps<"ol">, "children">`.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `steps` | yes | `StepperStep[]` |  |
| `variant` | no | `StepperVariant` | Which drawing. Defaults to `bar`. |
| `onStepClick` | no | `(id: string, index: number) => void` | Makes each step a button: completed steps and the current one are reachable, upcoming<br>ones disabled. Without it the steps are plain list items. |
| `strings` | no | `Partial<StepperStrings>` |  |

### `StepperStatus`

Kind: type.

Where a step stands: a position, not an outcome (a failed step is a Timeline entry).
`complete` is accepted as an alias and normalised to `completed`.

```text
"completed" | "complete" | "current" | "upcoming"
```

### `StepperStep`

Kind: interface.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `id` | yes | `string` | Stable identity for the step, handed back to `onStepClick`. |
| `label` | yes | `ReactNode` |  |
| `hint` | no | `ReactNode` | A second, quieter line under the label. |
| `status` | no | `StepperStatus` | Where the step stands. Defaults to `upcoming`. |
| `accessibleName` | no | `string` | The step's whole accessible name, replacing its announced position, label, hint and<br>state. `aria-current` still marks the current step. |

### `StepperStrings`

Kind: interface.

The stepper's copy — all for screen readers, since the markers are decoration.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `position` | yes | `(index: number, total: number) => string` | Says what the marker's numeral counts — "Step 2 of 4". |
| `completed` | yes | `string` | Said with a finished step, so the tick and its tone are not the only sign of it. |
| `current` | yes | `string` | Said with the step the reader is on, alongside `aria-current`. |

### `StepperVariant`

Kind: type.

`bar`: labels under ringed markers, a connector through the marker row, equal columns.
`trail`: labels beside filled markers and a rule filling the row; below `lg` only the
markers show.

```text
"bar" | "trail"
```

### `Timeline`

Kind: callable.

```text
({ items, className, ...props }: TimelineProps) => import("react").JSX.Element
```

### `TimelineItem`

Kind: interface.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `id` | yes | `string` | Stable identity for the row. |
| `title` | yes | `ReactNode` |  |
| `description` | no | `ReactNode` | Supporting copy under the title. |
| `timestamp` | no | `ReactNode` | Already formatted — the kit's date primitives decide the format, not this. |
| `trailing` | no | `ReactNode` | The title row's trailing lane when it holds something other than a time; replaces `timestamp`. |
| `icon` | no | `LucideIcon` | Decorative glyph inside the dot. |
| `status` | no | `TimelineStatus` |  |
| `children` | no | `ReactNode` | Anything the entry carries below its description — a card, a diff, an action row. |

### `TimelineProps`

Kind: interface.

Extends: `Omit<ComponentProps<"ol">, "children">`.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `items` | yes | `TimelineItem[]` |  |

### `TimelineStatus`

Kind: type.

Progress states first, then outcomes. `progress` is a position (a step walked past) and
takes the primary tone of `current`; `completed` is an outcome (a milestone that landed).
`pending` is the only unfilled dot.

```text
"progress" | "completed" | "current" | "pending" | "success" | "warning" | "destructive" | "neutral"
```

## Preview recipes

Preview route: Timeline — `/timeline`

### An order's progress

```tsx fragment — excerpt from the live preview; surrounding values are supplied by the application
<Timeline
  items={[
    { id: "placed", title: "Order placed", timestamp: "14 Aug, 09:12", icon: CheckIcon, status: "completed" },
    { id: "transit", title: "In transit", timestamp: "16 Aug, 06:02", icon: TruckIcon, status: "current" },
    { id: "delivered", title: "Delivered", timestamp: "Expected 18 Aug", status: "pending" },
  ]}
/>
```

### Statuses

```tsx fragment — excerpt from the live preview; surrounding values are supplied by the application
<Timeline items={[{ id: "1", title: "Packed", status: "completed" }]} />
```

### Entries that carry more than a line

```tsx fragment — excerpt from the live preview; surrounding values are supplied by the application
<Timeline
  items={[
    {
      id: "refund",
      title: "Refund issued",
      status: "warning",
      children: <Badge tone="warning">Partial</Badge>,
    },
  ]}
/>
```

### Props

```tsx fragment — excerpt from the live preview; surrounding values are supplied by the application
<Stepper
  steps={[
    { id: "account", label: "Account", hint: "Who you are", status: "completed" },
    { id: "shipping", label: "Shipping", status: "current" },
    { id: "review", label: "Review", status: "upcoming" },
  ]}
/>

<Stepper variant="trail" steps={steps} />
```

### Going back a step

```tsx fragment — excerpt from the live preview; surrounding values are supplied by the application
<Stepper
  variant="trail"
  steps={steps}
  onStepClick={(id, index) => setCurrent(index)}
/>
```
