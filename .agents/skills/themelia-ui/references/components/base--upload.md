<!-- GENERATED by scripts/gen-consumer-docs.mjs — do not edit. -->

# `base/upload`

The user contributes files — dropzone, avatar, image, or a tray with progress.

## Selection

- **Use when:** The user contributes files — dropzone, avatar, image, or a tray with progress.
- **Avoid when:** Existing assets are being browsed and picked rather than added.
- **Consider instead:** `features/media-library`
- **Stability:** stable.

## Import

```tsx fragment — import-only example
import { AvatarUpload } from "themelia-ui/base/upload"
import "themelia-ui/base/upload.css"
```

## Composition

This family composes `base/badge`, `base/buttons`, `base/display`, `base/feedback`.

Application policy—routing, fetching, persistence, permissions, and translation—stays
outside the package and arrives through the public props, callbacks, slots, or accessors below.

## Public declarations

### `AvatarUpload`

Kind: callable.

```text
({ strings, ...props }: AvatarUploadProps) => import("react").JSX.Element
```

### `AvatarUploadProps`

Kind: type.

```text
MediaUploadProps
```

### `defaultAvatarUploadStrings`

Kind: const.

```text
MediaUploadStrings
```

### `defaultDropzoneStrings`

Kind: const.

```text
DropzoneStrings
```

### `defaultFilePickerStrings`

Kind: const.

```text
FilePickerStrings
```

### `defaultFileUploadStrings`

Kind: const.

```text
FileUploadStrings
```

### `defaultImageUploadStrings`

Kind: const.

```text
MediaUploadStrings
```

### `defaultMediaGalleryStrings`

Kind: const.

```text
MediaGalleryStrings
```

### `defaultUploadProgressListStrings`

Kind: const.

```text
UploadProgressListStrings
```

### `defaultUploadTrayStrings`

Kind: const.

```text
UploadTrayStrings
```

### `Dropzone`

Kind: callable.

```text
({ onDrop, accept, multiple, disabled, invalid, compact, icon, inputProps, strings, label, hint, footer, className, ...props }: DropzoneProps) => import("react").JSX.Element
```

### `DropzoneProps`

Kind: interface.

Extends: `Omit<ComponentProps<"div">, "onDrop" | "children">`.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `onDrop` | yes | `(files: File[]) => void` | Called with what was dropped or picked. Nothing is validated first. |
| `accept` | no | `string` | MIME types or extensions, as the native input takes them. |
| `multiple` | no | `boolean` |  |
| `disabled` | no | `boolean` |  |
| `invalid` | no | `boolean` |  |
| `compact` | no | `boolean` | A shorter box, for a zone beside other fields rather than filling a panel. |
| `icon` | no | `ReactNode` | Replaces the cloud glyph. |
| `inputProps` | no | `Omit<ComponentProps<"input">, "type" \| "accept" \| "multiple" \| "disabled" \| "onChange">` | Native attributes for the input itself — `name`, `form`, `required`. |
| `strings` | no | `Partial<DropzoneStrings>` |  |
| `label` | no | `ReactNode` | Replaces the prompt — "Attach your receipts". Shorthand for `strings.instruction`. |
| `hint` | no | `ReactNode` | Replaces the second line — the accepted types, the size cap — with any node. |
| `footer` | no | `ReactNode` | Controls rendered below the label, not inside it, where clicks would open the file dialog. |

### `DropzoneStrings`

Kind: interface.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `instruction` | yes | `string` | The prompt, singular and plural (two keys, since plurals vary by language). |
| `instructionMultiple` | yes | `string` |  |
| `dragOver` | yes | `string` | Replaces the prompt while something is over the box. |
| `helper` | yes | `string` | A second line under the prompt — the accepted types, the size cap. |

### `FileConstraints`

Kind: interface.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `accept` | no | `string` | Native accept filter, e.g. `.pdf,image/*`. Enforced here as well as by the dialog. |
| `minSizeBytes` | no | `number` |  |
| `maxSizeBytes` | no | `number` |  |
| `validateFile` | no | `FileValidator` | Runs after the type and size checks. |

### `FileDropTargetProps`

Kind: interface.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `onDragEnter` | yes | `DragEventHandler<HTMLElement>` |  |
| `onDragOver` | yes | `DragEventHandler<HTMLElement>` |  |
| `onDragLeave` | yes | `DragEventHandler<HTMLElement>` |  |
| `onDrop` | yes | `DragEventHandler<HTMLElement>` |  |

### `FilePickerInput`

Kind: callable.

```text
({ value, onValueChange, onRejectedFiles, constraints, disabled, invalid, name, strings, className, ...props }: FilePickerInputProps) => import("react").JSX.Element
```

### `FilePickerInputProps`

Kind: interface.

Extends: `Omit<ComponentProps<"div">, "onChange" | "defaultValue">`.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `value` | no | `File \| null` | Controlled. |
| `onValueChange` | no | `(file: File \| undefined) => void` |  |
| `onRejectedFiles` | no | `(rejections: FileRejection[]) => void` |  |
| `constraints` | no | `FileConstraints` | Type and size limits. Re-checked here, not left to the file dialog. |
| `disabled` | no | `boolean` |  |
| `invalid` | no | `boolean` |  |
| `name` | no | `string` |  |
| `strings` | no | `StringsProp<FilePickerStrings>` |  |

### `FilePickerStrings`

Kind: interface.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `empty` | yes | `string` | Shown when nothing is chosen. |
| `clear` | yes | `string` |  |

### `FileRejection`

Kind: interface.

One refused file: a code and numbers, which `FileUploadStrings` turns into a sentence.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `file` | yes | `File` |  |
| `code` | yes | `FileRejectionCode` |  |
| `details` | yes | `FileRejectionDetails` |  |

### `FileRejectionCode`

Kind: type.

Stable, machine-readable reason a local file was refused.

```text
"invalid-type" | "too-small" | "too-large" | "too-many" | "duplicate" | "custom"
```

### `FileRejectionDetails`

Kind: interface.

The numbers behind a rejection, for the component to write copy from.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `limitBytes` | no | `number` | The bound that was exceeded, in bytes. |
| `maxFiles` | no | `number` | The file cap, for `too-many`. |
| `customMessage` | no | `string` | Only ever what a `FileValidator` returned. Nothing here is written by the kit. |

### `FileUpload`

Kind: callable.

```text
({ id, value, defaultValue, onValueChange, onRejectedFiles, multiple, maxFiles, selectionMode, disabled, invalid, compact, label, strings, hint, showList, showRejections, progress, name, className, "aria-labelledby": labelledBy, "aria-describedby": describedBy, "aria-invalid": ariaInvalid, "aria-required": ariaRequired, ...constraints }: FileUploadProps) => import("react").JSX.Element
```

### `FileUploadProps`

Kind: interface.

Extends: `FileConstraints`.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `id` | no | `string` | Applied to the file input itself, so a `FormField` label can address it. |
| `value` | no | `File[]` | Controlled selection. |
| `defaultValue` | no | `File[]` |  |
| `onValueChange` | no | `(files: File[]) => void` |  |
| `onRejectedFiles` | no | `(rejections: FileRejection[]) => void` | Receives what was refused. Rejected files never reach `onValueChange`. |
| `multiple` | no | `boolean` |  |
| `maxFiles` | no | `number` | Cap on the selection. Defaults to 1 for single, unlimited for multiple. |
| `selectionMode` | no | `"append" \| "replace"` | Whether a new selection adds to the list or replaces it. |
| `disabled` | no | `boolean` |  |
| `invalid` | no | `boolean` |  |
| `compact` | no | `boolean` | A compact single row, for a zone that sits inside a form rather than owning a page. |
| `label` | no | `ReactNode` | Shorthand for `strings.instruction`. |
| `strings` | no | `Partial<FileUploadStrings>` | Overrides this control's own copy, the rejection sentences included. |
| `hint` | no | `ReactNode` |  |
| `showList` | no | `boolean` | Hides the built-in list, for a caller rendering their own. |
| `showRejections` | no | `boolean` | Shows the refusals under the zone. |
| `progress` | no | `Record<string, number>` | Per-file transfer progress, 0–100, keyed by file name. |
| `name` | no | `string` |  |
| `className` | no | `string` |  |
| `"aria-labelledby"` | no | `string` |  |
| `"aria-describedby"` | no | `string` |  |
| `"aria-invalid"` | no | `boolean \| "true" \| "false"` |  |
| `"aria-required"` | no | `boolean \| "true" \| "false"` |  |

### `FileUploadStrings`

Kind: interface.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `instruction` | yes | `string` | The drop target's prompt, singular and plural (two keys, since plurals vary by language). |
| `instructionMultiple` | yes | `string` |  |
| `dragOver` | no | `string` | Replaces the prompt while a file is over the zone. Falls back to Dropzone's own. |
| `uploading` | yes | `(name: string) => string` | Announced while a transfer is in flight, named after the file. |
| `remove` | yes | `(name: string) => string` | Names each row's remove control, after the file it removes. |
| `rejection` | yes | `(rejection: FileRejection) => string` | One refused file as a sentence, from the whole rejection (file, code, bound). |
| `constraintHint` | no | `(constraints: Pick<FileConstraints, "accept" \| "maxSizeBytes">) => string` | The hint built from the constraints when no `hint` is given ("image/* · up to 5 MB");<br>empty for none. Optional so older complete strings objects still type-check. |

### `FileValidator`

Kind: type.

Return `true` to accept, `false` for the default message, or a message of your own.

```text
(file: File) => boolean | string
```

### `ImageUpload`

Kind: callable.

```text
({ strings, ...props }: ImageUploadProps) => import("react").JSX.Element
```

### `ImageUploadProps`

Kind: type.

```text
MediaUploadProps
```

### `MediaGallery`

Kind: callable.

```text
({ value, defaultValue, onValueChange, onRejectedFiles, maxFiles, disabled, invalid, showCover, strings, accept, ...constraints }: MediaGalleryProps & { className?: string; }) => import("react").JSX.Element
```

### `MediaGalleryProps`

Kind: interface.

Extends: `FileConstraints`.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `value` | no | `File[]` | Controlled. The order is part of the value: the first entry is the cover. |
| `defaultValue` | no | `File[]` |  |
| `onValueChange` | no | `(files: File[]) => void` |  |
| `onRejectedFiles` | no | `(rejections: FileRejection[]) => void` |  |
| `maxFiles` | no | `number` |  |
| `disabled` | no | `boolean` |  |
| `invalid` | no | `boolean` |  |
| `showCover` | no | `boolean` | Badges the first tile as the cover. Defaults to true; turn off when order carries no meaning. |
| `strings` | no | `Partial<MediaGalleryStrings>` | Overrides this gallery's own copy. |
| `className` | no | `string` |  |

### `MediaGalleryStrings`

Kind: interface.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `cover` | yes | `string` | Badge on the first tile (hidden with `showCover={false}`). |
| `add` | yes | `string` | The tile that opens the file picker. |
| `position` | yes | `(name: string, index: number, total: number) => string` | Names a tile, with its position and how to move it. |
| `remove` | yes | `(name: string) => string` | Names the control that removes one tile, after its file. |

### `MediaUploadStrings`

Kind: interface.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `empty` | yes | `string` | Shown in the empty frame, and again over a filled one. |
| `overlay` | yes | `string` |  |
| `remove` | yes | `string` | Names the control that discards the current file. |

### `PreviewImage`

Kind: callable.

```text
({ src, alt, fallback, className }: PreviewImageProps) => import("react").JSX.Element
```

### `PreviewImageProps`

Kind: interface.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `src` | yes | `string` |  |
| `alt` | no | `string` | Empty by default: a thumbnail beside its own filename is decorative. |
| `fallback` | yes | `ReactNode` |  |
| `className` | no | `string` |  |

### `UploadItem`

Kind: interface.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `id` | yes | `string` | Stable identity. The caller's, not a file name — two files may share one. |
| `name` | yes | `string` |  |
| `size` | no | `number` | In bytes. Formatted for display here. |
| `progress` | no | `number` | 0–100. Ignored unless the status is `uploading`. |
| `status` | yes | `UploadStatus` |  |
| `error` | no | `string` | Shown under the row when the status is `error`. |
| `preview` | no | `string` | A thumbnail, used in place of the status glyph. |

### `UploadProgressList`

Kind: callable.

```text
({ items, onCancel, onRetry, onRemove, strings, className, }: UploadProgressListProps) => import("react").JSX.Element | null
```

### `UploadProgressListProps`

Kind: interface.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `items` | yes | `UploadItem[]` |  |
| `strings` | no | `Partial<UploadProgressListStrings>` | Overrides this list's own copy — the five status words and the row controls. |
| `onCancel` | no | `(id: string) => void` | Offered while a row is queued or uploading. |
| `onRetry` | no | `(id: string) => void` | Offered when a row has failed. |
| `onRemove` | no | `(id: string) => void` | Offered once a row is done, cancelled or failed, and on a queued row without<br>`onCancel`. Never on a transfer in flight (that is cancel). |
| `className` | no | `string` |  |

### `UploadProgressListStrings`

Kind: interface.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `status` | yes | `Record<"queued" \| "uploading" \| "done" \| "error" \| "cancelled", string>` | A word per row status; keyed by status, so a new status is a type error until named. |
| `cancel` | yes | `(name: string) => string` | The three row controls, each named after the file it acts on. |
| `retry` | yes | `(name: string) => string` |  |
| `remove` | yes | `(name: string) => string` |  |
| `uploading` | yes | `(name: string) => string` | Names the progress bar of a row that is mid-upload. |
| `settled` | no | `(name: string, status: string) => string` | Announced when a row finishes (uploaded, failed or cancelled). Optional so older<br>complete strings objects still type-check. |

### `UploadStatus`

Kind: type.

```text
"queued" | "uploading" | "done" | "error" | "cancelled"
```

### `UploadTray`

Kind: callable.

```text
({ items, onAddFiles, onCancel, onRetry, onRemove, onClearAll, strings, showSummary, className, ...uploadProps }: UploadTrayProps) => import("react").JSX.Element
```

### `UploadTrayProps`

Kind: interface.

Extends: `Omit<FileUploadProps, "value" | "onValueChange" | "showList">`.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `items` | yes | `UploadItem[]` | Everything in the tray, at whatever stage. |
| `onAddFiles` | yes | `(files: File[]) => void` | Fires when files are dropped or browsed; the caller adds them to `items` and starts the transfer. |
| `onCancel` | no | `(id: string) => void` |  |
| `onRetry` | no | `(id: string) => void` |  |
| `onRemove` | no | `(id: string) => void` |  |
| `onClearAll` | no | `() => void` |  |
| `strings` | no | `Partial<UploadTrayStrings>` | Overrides this tray's own copy — the clear-all control. |
| `showSummary` | no | `boolean` | Hides the counts row. |

### `UploadTrayStrings`

Kind: interface.

Extends: `FileUploadStrings`, `UploadProgressListStrings`.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `clearAll` | yes | `string` | The control that empties the tray. |

### `useFileDropTarget`

Kind: callable.

Drag state that survives descendants: `dragleave` fires on entering a child, so enters
and leaves are counted and dragging clears only at zero.

```text
({ disabled, onFiles, }: { disabled?: boolean; onFiles: (files: File[]) => void; }) => { isDragging: boolean; dropTargetProps: { onDragEnter: DragEventHandler<HTMLElement>; onDragOver: DragEventHandler<HTMLElement>; onDragLeave: DragEventHandler<HTMLElement>; onDrop: DragEventHandler<HTMLElement>; }; }
```

### `validateFileSelection`

Kind: callable.

Validates a selection; each file gets its own verdict, so one rejection does not stop the rest.

```text
({ incoming, current, maxFiles, append, ...constraints }: ValidateOptions) => ValidationResult
```

### `ValidateOptions`

Kind: interface.

Extends: `FileConstraints`.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `incoming` | yes | `File[]` |  |
| `current` | yes | `File[]` |  |
| `maxFiles` | yes | `number` |  |
| `append` | yes | `boolean` | Adds to the current list rather than replacing it. |

### `ValidationResult`

Kind: interface.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `accepted` | yes | `File[]` |  |
| `nextFiles` | yes | `File[]` |  |
| `rejections` | yes | `FileRejection[]` |  |

## Preview recipes

Preview route: File upload — `/file-upload`

### FileUpload

```tsx fragment — excerpt from the live preview; surrounding values are supplied by the application
<FileUpload
  multiple
  accept=".pdf,image/*"
  maxSizeBytes={5 * 1024 * 1024}
  maxFiles={4}
  value={files}
  onValueChange={setFiles}
  onRejectedFiles={setRejected}
/>
```

### Validation is not the dialog's job

```tsx fragment — excerpt from the live preview; surrounding values are supplied by the application
<AvatarUpload onValueChange={setAvatar} />
<ImageUpload previewUrl={coverUrl} onValueChange={setCover} />
```

### Files on their way: upload progress

```tsx fragment — excerpt from the live preview; surrounding values are supplied by the application
<UploadProgressList items={items} onRetry={retry} onRemove={remove} />
```

### UploadTray

```tsx fragment — excerpt from the live preview; surrounding values are supplied by the application
<UploadTray
  items={items}
  onAddFiles={start}
  onRetry={retry}
  onRemove={remove}
  onClearAll={clear}
/>
```

### MediaGallery

```tsx fragment — excerpt from the live preview; surrounding values are supplied by the application
<MediaGallery
  value={images}
  onValueChange={setImages}
  maxFiles={6}
/>
```
