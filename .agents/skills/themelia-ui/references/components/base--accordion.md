<!-- GENERATED by scripts/gen-consumer-docs.mjs — do not edit. -->

# `base/accordion`

Several titled sections share one column and the reader opens them one at a time.

## Selection

- **Use when:** Several titled sections share one column and the reader opens them one at a time.
- **Avoid when:** Only one section can collapse, or the panel is a disclosure attached to a control.
- **Consider instead:** `base/display`
- **Stability:** stable.

## Import

```tsx fragment — import-only example
import { Accordion } from "themelia-ui/base/accordion"
import "themelia-ui/base/accordion.css"
```

## Composition

This family composes `base/typography`.

Application policy—routing, fetching, persistence, permissions, and translation—stays
outside the package and arrives through the public props, callbacks, slots, or accessors below.

## Public declarations

### `Accordion`

Kind: callable.

```text
({ className, surface, items, media, children, ...props }: AccordionProps) => import("react").JSX.Element
```

### `AccordionContent`

Kind: callable.

```text
({ className, children, ...props }: AccordionContentProps) => import("react").JSX.Element
```

### `AccordionContentProps`

Kind: type.

```text
AccordionPrimitive.Panel.Props
```

### `AccordionItem`

Kind: callable.

```text
({ className, ...props }: AccordionItemProps) => import("react").JSX.Element
```

### `AccordionItemData`

Kind: interface.

One section for the bounded `items` API; use the compound parts for richer rows.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `value` | yes | `string` | Stable identifier. Used as the item's `value`. |
| `title` | yes | `ReactNode` |  |
| `description` | no | `ReactNode` | Supporting line below the title, inside the trigger. |
| `icon` | no | `ReactNode` | Leading media. Framed per the root's `media` prop. |
| `badge` | no | `ReactNode` | Trailing node after the title — usually a `<Badge>`. |
| `content` | yes | `ReactNode` |  |
| `disabled` | no | `boolean` |  |

### `AccordionItemProps`

Kind: type.

```text
AccordionPrimitive.Item.Props
```

### `AccordionMedia`

Kind: type.

How leading media is framed. With no media in any row the column is dropped; a mixed
group reserves it on every row so titles align.

```text
"inline" | "medallion" | "none"
```

### `AccordionProps`

Kind: interface.

Extends: `AccordionPrimitive.Root.Props<string>`.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `surface` | no | `AccordionSurface` | Outer chrome. Resolves through the provider when omitted. |
| `items` | no | `AccordionItemData[]` | Bounded sections, each rendered as the canonical icon/title/badge/description row.<br>Ignored when `children` are supplied. |
| `media` | no | `AccordionMedia` | How leading media is framed. Only meaningful alongside `items`. |

### `AccordionSurface`

Kind: type.

Outer chrome for the group: `bordered` — one shell with quiet dividers (the settings
default); `card` — a panel per section; `flat` — none, the consumer supplies it.

```text
"bordered" | "card" | "flat"
```

### `AccordionTrigger`

Kind: callable.

```text
({ className, children, ...props }: AccordionTriggerProps) => import("react").JSX.Element
```

### `AccordionTriggerProps`

Kind: type.

```text
AccordionPrimitive.Trigger.Props
```

## Preview recipes

Preview route: Accordion — `/accordion`

### Accordion

```tsx fragment — excerpt from the live preview; surrounding values are supplied by the application
<Accordion
  items={[
    { value: "billing", title: "Billing", description: "…", content: <…/> },
  ]}
/>
```
