<!-- GENERATED by scripts/gen-consumer-docs.mjs — do not edit. -->

# `base/toaster`

A message is transient, non-blocking, and reports the outcome of an action.

## Selection

- **Use when:** A message is transient, non-blocking, and reports the outcome of an action.
- **Avoid when:** The message belongs to a region, or the user must act on it.
- **Consider instead:** `base/feedback`, `features/overlays`
- **Stability:** stable.

## Import

```tsx fragment — import-only example
import { Toaster } from "themelia-ui/base/toaster"
import "themelia-ui/base/toaster.css"
```

## Composition

This family has no component-family dependencies.

Application policy—routing, fetching, persistence, permissions, and translation—stays
outside the package and arrives through the public props, callbacks, slots, or accessors below.

## Public declarations

### `createToastStore`

Kind: callable.

A store with its own queue and timers, for a second, isolated Toaster (a host app and an
embedded widget). Most apps use the default `toastStore`.

```text
() => ToastStore
```

### `defaultToasterStrings`

Kind: const.

```text
ToasterStrings
```

### `dismissToast`

Kind: callable.

```text
(id?: string) => void
```

### `toast`

Kind: const.

```text
ToastFn
```

### `ToastAction`

Kind: interface.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `label` | yes | `string` |  |
| `onClick` | yes | `() => void` |  |

### `Toaster`

Kind: callable.

Mount once, near the application root. The store lives at module scope so `toast()` works
from anywhere; this component is only the view.

```text
({ position, duration, visibleToasts, closeButton, strings, className, container, store, }: ToasterProps) => import('react').ReactPortal | null
```

### `ToasterProps`

Kind: interface.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `position` | no | `ToastPosition` |  |
| `duration` | no | `number` | Default lifetime in ms. A toast with its own `duration` still wins. |
| `visibleToasts` | no | `number` | Maximum toasts on screen. Older ones are dropped from the render, not the store. |
| `closeButton` | no | `boolean` |  |
| `strings` | no | `Partial<ToasterStrings>` | Overrides this region's own copy — its name, and each toast's dismiss. |
| `className` | no | `string` |  |
| `store` | no | `ToastStore` | The queue this Toaster renders. Defaults to the singleton `toast()` writes to; pass a<br>`createToastStore()` instance for independent Toasters (a host app and an embedded widget). |
| `container` | no | `UIPortalContainer` | Where the toast region renders. Defaults to the nearest `UIPortalHost`, else<br>`document.body`, so scoped density and theme apply. |

### `ToasterStrings`

Kind: interface.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `label` | yes | `string` | Names the live region the toasts are announced from. |
| `dismiss` | yes | `string` | Names each toast's dismiss control, which is icon-only. |

### `ToastOptions`

Kind: interface.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `id` | no | `string` |  |
| `description` | no | `ReactNode` |  |
| `icon` | no | `ReactNode` | Replaces the status glyph. |
| `duration` | no | `number` | Milliseconds before auto-dismissal. `Infinity` pins it open. |
| `action` | no | `ToastAction` |  |
| `cancel` | no | `ToastAction` |  |
| `onDismiss` | no | `(id: string) => void` | Called after the toast leaves, whichever way it left. |

### `ToastPosition`

Kind: type.

```text
"top-start" | "top-center" | "top-end" | "bottom-start" | "bottom-center" | "bottom-end"
```

### `ToastRecord`

Kind: interface.

Extends: `Omit<ToastOptions, "id">`.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `id` | yes | `string` |  |
| `title` | yes | `ReactNode` |  |
| `status` | yes | `ToastStatus` |  |
| `leaving` | no | `boolean` | Set while the exit transition runs, so the DOM node survives long enough to animate. |

### `ToastStatus`

Kind: type.

Status: the surface is the same for all; only the glyph and announcement politeness change.

```text
"neutral" | "success" | "info" | "warning" | "error" | "loading"
```

### `ToastStore`

Kind: interface.

One queue of toasts, its timers, and the `toast()` bound to it.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `subscribe` | yes | `() => void` |  |
| `getSnapshot` | yes | `ToastRecord[]` |  |
| `getServerSnapshot` | yes | `ToastRecord[]` |  |
| `toast` | yes | `ToastFn` | Raise a toast on this store. `toast` is the ergonomic form of the same thing. |
| `dismiss` | yes | `void` |  |
| `pauseAll` | yes | `void` | Pause every running timer — pointer-enter and focus-within. |
| `resumeAll` | yes | `void` |  |
| `applyDefaultDuration` | yes | `void` | Give the Toaster's configured default to anything queued without one. |

## Preview recipes

Preview route: Toast — `/toast`

### Statuses

```tsx fragment — excerpt from the live preview; surrounding values are supplied by the application
toast.success("Invoice sent")
toast.error("Could not reach the server")
toast.loading("Uploading…", { id: "upload" })
```

### Actions

```tsx fragment — excerpt from the live preview; surrounding values are supplied by the application
toast("Invoice deleted", {
  action: { label: "Undo", onClick: restore },
  duration: 8000,
})
```

### Promise

```tsx fragment — excerpt from the live preview; surrounding values are supplied by the application
toast.promise(saveInvoice(), {
  loading: "Saving…",
  success: (invoice) => `Saved ${invoice.id}`,
  error: "Could not save",
})
```
