<!-- GENERATED by scripts/gen-consumer-docs.mjs — do not edit. -->

# `features/sync`

A synchronisation run is configured over a date range.

## Selection

- **Use when:** A synchronisation run is configured over a date range.
- **Avoid when:** The form is a general-purpose date range field.
- **Consider instead:** `base/date-pickers`
- **Stability:** stable.

## Import

```tsx fragment — import-only example
import { SyncRangeForm } from "themelia-ui/features/sync"
import "themelia-ui/features/sync.css"
```

## Optional peers

Install these only when importing this family:

```bash
npm install react-hook-form
```

## Composition

This family composes `base/choice-inputs`, `base/label`, `base/structure`, `base/typography`.

Application policy—routing, fetching, persistence, permissions, and translation—stays
outside the package and arrives through the public props, callbacks, slots, or accessors below.

## Public declarations

### `defaultSyncRangeFormStrings`

Kind: const.

```text
SyncRangeFormStrings
```

### `SyncRangeForm`

Kind: callable.

```text
({ formId, options, syncOptions, value, defaultValue, onValueChange, transformSubmit, onSubmit, onError, resetKey, disabled, submitting, errors, strings, className, }: SyncRangeFormProps<TSubmit>) => import("react").JSX.Element
```

### `SyncRangeFormProps`

Kind: interface.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `formId` | yes | `string` | Set on the `<form>` so the overlay's footer, outside it, can submit it. |
| `options` | yes | `CardRadioOption[]` | The windows offered. |
| `syncOptions` | no | `CardCheckboxOption[]` |  |
| `value` | no | `SyncRangeFormValues` | Controlled. |
| `defaultValue` | no | `Partial<SyncRangeFormValues>` |  |
| `onValueChange` | no | `(value: SyncRangeFormValues) => void` |  |
| `transformSubmit` | no | `(value: SyncRangeFormValues) => TSubmit` | Maps the form's values to the consumer's payload. Replaces the numeric default. |
| `onSubmit` | yes | `(data: TSubmit, values: SyncRangeFormValues) => void \| Promise<void>` |  |
| `onError` | no | `(error: unknown) => void` |  |
| `resetKey` | no | `string \| number` | Changing it resets an uncontrolled form, e.g. after a mutation or for a new record. |
| `disabled` | no | `boolean` |  |
| `submitting` | no | `boolean` |  |
| `errors` | no | `Record<string, string \| string[] \| undefined>` | Server-side messages by field. An array is reduced to its first entry. |
| `strings` | no | `Partial<SyncRangeFormStrings>` |  |
| `className` | no | `string` |  |

### `SyncRangeFormStrings`

Kind: interface.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `hoursLabel` | yes | `string` |  |
| `hoursDescription` | yes | `string` |  |
| `optionsLabel` | yes | `string` |  |
| `optionsDescription` | yes | `string` |  |
| `hoursRequired` | yes | `string` | Shown when no window is chosen and the form is submitted. |

### `SyncRangeFormSubmit`

Kind: interface.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `hours` | yes | `number` |  |
| `options` | yes | `string[]` |  |

### `SyncRangeFormValues`

Kind: interface.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `hours` | yes | `string` | The chosen window's id. Numeric strings work with the default transform. |
| `options` | yes | `string[]` | Every ticked option, in the order the group reports them. |

## Preview recipes

Preview route: Kanban & sync — `/kanban`

### Where the handle goes

```tsx fragment — excerpt from the live preview; surrounding values are supplied by the application
<ActionDialog
  title="Run a sync"
  formId="sync-form"
  trigger={<Button>Sync</Button>}
>
  <SyncRangeForm
    formId="sync-form"
    options={windows}
    syncOptions={families}
    onSubmit={({ hours, options }) => api.sync(hours, options)}
  />
</ActionDialog>
```
