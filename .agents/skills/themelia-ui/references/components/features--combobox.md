<!-- GENERATED by scripts/gen-consumer-docs.mjs — do not edit. -->

# `features/combobox`

Options arrive from a fetch, the field selects application resources, or it proposes completions as the user types.

## Selection

- **Use when:** Options arrive from a fetch, the field selects application resources, or it proposes completions as the user types.
- **Avoid when:** The options are a small in-memory list, you are composing a bespoke anatomy, or a trigger character inserts entities into text.
- **Consider instead:** `base/combobox`, `base/choice-inputs`, `features/mentions`
- **Stability:** stable.

## Import

```tsx fragment — import-only example
import { AsyncCombobox } from "themelia-ui/features/combobox"
import "themelia-ui/features/combobox.css"
```

## Composition

This family composes `base/buttons`, `base/combobox`, `base/display`, `base/feedback`, `base/spinner`, `base/typography`.

Application policy—routing, fetching, persistence, permissions, and translation—stays
outside the package and arrives through the public props, callbacks, slots, or accessors below.

## Public declarations

### `AsyncCombobox`

Kind: callable.

```text
({ selectedValue, onSelectedValueChange, clearSearchOnClose, minSearchLength, loading, showListContent, disabled, clearable, ...shared }: AsyncComboboxProps<T>) => import("react").JSX.Element
```

### `AsyncComboboxProps`

Kind: interface.

Extends: `AsyncComboboxSharedProps<T>`.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `selectedValue` | yes | `T \| null` | The selected item (not a key), or `null`. Controlled; survives `items` being replaced. |
| `onSelectedValueChange` | yes | `(value: T \| null) => void` |  |
| `clearSearchOnClose` | no | `boolean` | Clears the query when the popup closes. Defaults to `true`. |

### `AsyncComboboxSharedProps`

Kind: interface.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `id` | no | `string` | Applied to the search input (not a wrapper), so a `FormField` label can target it. |
| `items` | yes | `T[]` | In display order. Rendered as given, never filtered. |
| `searchValue` | yes | `string` | The text in the field. Controlled. |
| `onSearchValueChange` | yes | `(value: string) => void` | Every keystroke, with the raw untrimmed value. |
| `getItemLabel` | yes | `(item: T) => string` | The visible label. Also the identity when `getItemKey` is absent, and the string the<br>create row's duplicate check compares against. |
| `getItemKey` | no | `(item: T) => string` | Stable identity. Defaults to the label; supply it whenever labels are not unique. |
| `loading` | no | `boolean` |  |
| `showListContent` | no | `boolean` | Whether list-owned content (status, results, create row, footer) may render. Defaults to<br>`true`. Turning it off does not clear the selection. |
| `minSearchLength` | no | `number` | Characters required before the list becomes active. Defaults to 3. Below it the status<br>row replaces results and `onSearch` does not fire; `0` allows browsing before typing. |
| `strings` | no | `Partial<AsyncComboboxStrings>` |  |
| `disabled` | no | `boolean` |  |
| `className` | no | `string` |  |
| `clearable` | no | `boolean` |  |
| `renderItem` | no | `(item: T) => ReactNode` | Replaces an option's content. Takes precedence over `highlightMatch`. |
| `portalContainer` | no | `RefObject<HTMLElement \| null>` | Mounts the popup inside this element instead of the default portal root. |
| `name` | no | `string` |  |
| `invalid` | no | `boolean` |  |
| `error` | no | `string` | A validation message under the field. Also marks it invalid. |
| `required` | no | `boolean` |  |
| `onBlur` | no | `() => void` |  |
| `onSearch` | no | `(value: string) => void` | The server query: fires after `debounceMs` with the trimmed value, only at or above<br>`minSearchLength`. To clear results below the threshold, use `onSearchValueChange`. |
| `debounceMs` | no | `number` |  |
| `creatable` | no | `boolean` | Offers an inline create row when nothing matches. Needs `onCreate` to do anything. |
| `onCreate` | no | `(value: string) => void` |  |
| `getItemGroup` | no | `(item: T) => string` | Returns a group name per item. Absent means no grouping. |
| `renderGroupLabel` | no | `(group: string) => ReactNode` |  |
| `hasMore` | no | `boolean` | Enables scroll-triggered pagination. |
| `onLoadMore` | no | `() => void` |  |
| `loadingMore` | no | `boolean` |  |
| `getItemDisabled` | no | `(item: T) => boolean` | Renders an option unselectable without hiding it. |
| `highlightMatch` | no | `boolean` | Emphasises the matched substring. Ignored when `renderItem` is set. |
| `open` | no | `boolean` |  |
| `onOpenChange` | no | `(open: boolean) => void` |  |

### `AsyncComboboxStrings`

Kind: interface.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `placeholder` | yes | `string` |  |
| `clear` | yes | `string` | Accessible name for the single-select field's clear control. |
| `toggle` | yes | `string` | Accessible name for the single-select field's chevron. |
| `searching` | yes | `string` |  |
| `noResults` | yes | `string` |  |
| `formatTypeToSearch` | yes | `(minimum: number) => string` | Takes the threshold. The default omits the count at a threshold of one. |
| `formatTypeMore` | yes | `(remaining: number) => string` | Takes how many characters are still missing. Plurals are the language's business. |
| `formatCreate` | yes | `(value: string) => string` | Takes the typed text, so "Create “blue”" can become whatever the language needs. |
| `loadingMore` | yes | `string` |  |
| `apply` | yes | `string` |  |
| `cancel` | yes | `string` |  |
| `errorTitle` | yes | `string` | Announced when a fetch fails, above the retry control. |
| `retry` | yes | `string` |  |
| `removeChip` | no | `(label: string) => string` | Accessible name for a multi-select chip's remove control. Takes the chip's label. Optional for compatibility; the default supplies it. |

### `AsyncMultiCombobox`

Kind: callable.

```text
({ selectedValues, onSelectedValuesChange, minSearchLength, loading, showListContent, disabled, closeOnSelect, applyButton, onApply, onCancel, open, onOpenChange, ...shared }: AsyncMultiComboboxProps<T>) => import("react").JSX.Element
```

### `AsyncMultiComboboxProps`

Kind: interface.

Extends: `AsyncComboboxSharedProps<T>`.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `selectedValues` | yes | `T[]` | The selections. Controlled; merged ahead of `items` and de-duplicated by key so they stay visible. |
| `onSelectedValuesChange` | yes | `(values: T[]) => void` | With `applyButton`, this fires only on Apply. Without it, on every toggle. |
| `closeOnSelect` | no | `boolean` | Defaults to false. In apply mode the list stays open until Apply or Cancel. |
| `applyButton` | no | `boolean` | Renders an Apply / Cancel footer and holds edits in a draft until Apply. Without it every toggle commits. |
| `onApply` | no | `() => void` |  |
| `onCancel` | no | `() => void` |  |

### `ComboboxDropdown`

Kind: callable.

```text
({ loading, showStatus, showEmpty, trimmedSearch, minSearchLength, items, groupedItems, createOptionItem, renderItemContent, getItemReactKey, getItemDisabled, renderGroupLabel, loadingMore, strings, portalContainer, listRef, anchor, applyFooter, errorSlot, }: ComboboxDropdownProps<T>) => import("react").JSX.Element
```

### `ComboboxDropdownProps`

Kind: interface.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `loading` | yes | `boolean` |  |
| `showStatus` | yes | `boolean` |  |
| `showEmpty` | yes | `boolean` |  |
| `trimmedSearch` | yes | `string` |  |
| `minSearchLength` | yes | `number` |  |
| `items` | yes | `T[]` |  |
| `groupedItems` | yes | `Map<string, T[]> \| null` |  |
| `createOptionItem` | yes | `T \| null` |  |
| `renderItemContent` | yes | `(item: T) => ReactNode` |  |
| `getItemReactKey` | yes | `(item: T) => string` |  |
| `getItemDisabled` | no | `(item: T) => boolean` |  |
| `renderGroupLabel` | no | `(group: string) => ReactNode` |  |
| `loadingMore` | no | `boolean` |  |
| `strings` | yes | `AsyncComboboxStrings` |  |
| `portalContainer` | no | `RefObject<HTMLElement \| null>` |  |
| `listRef` | yes | `RefObject<HTMLDivElement \| null>` | The scrollable viewport, watched for pagination. |
| `anchor` | no | `RefObject<HTMLElement \| null>` | What the popup positions against. The chips row, for a multi-select. |
| `applyFooter` | no | `{ applyLabel: string; cancelLabel: string; onApply: () => void; onCancel: () => void; }` |  |
| `errorSlot` | no | `ReactNode` | Error content, shown instead of everything else. |

### `DEFAULT_MIN_SEARCH_LENGTH`

Kind: const.

```text
unknown
```

### `defaultAsyncComboboxStrings`

Kind: const.

```text
AsyncComboboxStrings
```

### `defaultSuggestionsStrings`

Kind: const.

```text
SuggestionsStrings
```

### `HighlightedText`

Kind: callable.

```text
({ text, highlight }: { text: string; highlight: string; }) => import("react").JSX.Element
```

### `MaybePromise`

Kind: type.

```text
T | Promise<T>
```

### `ResourceCombobox`

Kind: callable.

```text
({ fetcher, onAfterFetch, onError, getKey, getLabel, limit, debounceMs, preload, minSearchLength, defaultValue, errorMessage, ...props }: ResourceComboboxProps<T>) => import("react").JSX.Element
```

### `ResourceComboboxErrorContext`

Kind: interface.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `error` | yes | `unknown` |  |
| `query` | yes | `string` | The query that failed, so a retry asks the same question. |
| `retry` | yes | `() => void` |  |

### `ResourceComboboxFetcher`

Kind: type.

```text
(args: ResourceComboboxFetcherArgs) => Promise<T[]>
```

### `ResourceComboboxFetcherArgs`

Kind: interface.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `query` | yes | `string` | Trimmed. Empty when preloading. |
| `limit` | yes | `number` | Advisory — nothing truncates the returned array. |
| `signal` | no | `AbortSignal` | Aborted when a newer request starts, or on unmount. Pass it to fetch. |

### `ResourceComboboxOptionContext`

Kind: interface.

What a custom option renderer is told about the row it is drawing.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `query` | yes | `string` |  |
| `isSelected` | yes | `boolean` |  |

### `ResourceComboboxProps`

Kind: interface.

Extends: `Omit<AsyncComboboxProps<T>, "items" | "selectedValue" | "onSelectedValueChange" | "clearSearchOnClose" | "searchValue" | "onSearchValueChange" | "getItemLabel" | "getItemKey" | "loading" | "showListContent" | "renderItem" | "error">`.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `fetcher` | yes | `ResourceComboboxFetcher<T>` |  |
| `value` | no | `T \| null` | Controlled. Omit entirely to let the component own the selection. |
| `defaultValue` | no | `T \| null` | The starting selection when uncontrolled. Ignored once `value` is passed. |
| `onValueChange` | no | `(value: T \| null) => void` |  |
| `searchValue` | no | `string` | The query. Controlled (e.g. from the URL); omit to let the component own it. |
| `defaultSearchValue` | no | `string` | The starting query when uncontrolled. |
| `onSearchValueChange` | no | `(value: string) => void` | Every keystroke, with the raw untrimmed value. |
| `defaultOpen` | no | `boolean` | The popup's starting state when `open` is not controlled. |
| `clearSearchOnClose` | no | `boolean` | Clears the query when the popup closes. Defaults to `false`. A failed query is always kept for retry. |
| `requestDelay` | no | `number` | A further wait before the fetcher runs, after the loading state is showing. For rate-limited endpoints. |
| `onAfterFetch` | no | `(items: T[], context: ResourceComboboxFetcherArgs) => void` |  |
| `onError` | no | `(error: unknown, context: ResourceComboboxFetcherArgs) => void` | Aborted and superseded requests never reach this. |
| `getKey` | yes | `(item: T) => string` | Stable identity. Also decides which option reads as selected. |
| `getLabel` | yes | `(item: T) => string` |  |
| `getDescription` | no | `(item: T) => ReactNode` | Second line beside the label — what distinguishes two rows of one name. |
| `getMeta` | no | `(item: T) => ReactNode` | Third, quieter still. |
| `renderOption` | no | `(item: T, context: ResourceComboboxOptionContext) => ReactNode` | Replaces the whole option row. `getLabel` still supplies the input's text. |
| `limit` | no | `number` |  |
| `minSearchLength` | no | `number` | Characters required before fetching. Defaults to `0` (unlike `AsyncCombobox`'s `3`); an empty input follows `preload`. |
| `preload` | no | `boolean` | Fetches with an empty query, so there is something to look at before typing. |
| `errorMessage` | no | `ReactNode` |  |
| `renderError` | no | `(context: ResourceComboboxErrorContext) => ReactNode` |  |
| `strings` | no | `Partial<AsyncComboboxStrings>` |  |

### `SuggestionsCombobox`

Kind: callable.

```text
({ fetchData, itemKey, itemText, renderItem, minQueryLength, preload, query, defaultQuery, onQueryChange, allowClear, clearInputOnClose, disabled, strings, ...props }: SuggestionsComboboxProps<T>) => import("react").JSX.Element
```

### `SuggestionsComboboxProps`

Kind: interface.

Extends: `UseSuggestionsConfig<T>`.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `id` | no | `string` | Applied to the search input, so a `FormField` label can address it. |
| `itemKey` | yes | `(item: T) => string \| number` |  |
| `itemText` | yes | `(item: T) => string` |  |
| `renderItem` | no | `(item: T) => ReactNode` |  |
| `renderError` | no | `(context: SuggestionsErrorContext) => ReactNode` |  |
| `allowClear` | no | `boolean` |  |
| `clearInputOnClose` | no | `boolean` | Resets the query when the list closes. A query that failed is kept, for the retry. |
| `disabled` | no | `boolean` |  |
| `className` | no | `string` |  |
| `strings` | no | `Partial<SuggestionsStrings>` |  |
| `name` | no | `string` | The form field name, for native form submission. |
| `required` | no | `boolean` |  |
| `invalid` | no | `boolean` |  |
| `onBlur` | no | `() => void` |  |
| `portalContainer` | no | `RefObject<HTMLElement \| null>` | Mounts the popup inside this element instead of the default portal root. |
| `highlightMatch` | no | `boolean` | Emphasises the matched substring. Ignored when `renderItem` is set. |

### `SuggestionsErrorContext`

Kind: interface.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `error` | yes | `unknown` |  |
| `query` | yes | `string` |  |
| `retry` | yes | `() => void` |  |

### `SuggestionsFetchContext`

Kind: interface.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `signal` | no | `AbortSignal` | Aborted when a newer query starts or the surface unmounts. Pass it to fetch. |

### `SuggestionsStrings`

Kind: interface.

The copy `SuggestionsCombobox` speaks, mapped onto `AsyncComboboxStrings` inside it.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `placeholder` | yes | `string` |  |
| `emptyMessage` | yes | `string` |  |
| `loadingMessage` | yes | `string` |  |
| `startTypingMessage` | yes | `string` | Shown before anything has been typed. |
| `formatTypeMore` | no | `(remaining: number) => string` | Shown when something but not enough has been typed (`minQueryLength` above one). Takes<br>the characters still missing. Optional; when absent a supplied `startTypingMessage` is used. |
| `errorMessage` | yes | `string` |  |
| `retry` | yes | `string` |  |

### `useComboboxCore`

Kind: callable.

```text
({ items, searchValue, getItemLabel, getItemKey, loading, showListContent, minSearchLength, strings, renderItem, onSearch, debounceMs, creatable, onCreate, getItemGroup, hasMore, onLoadMore, loadingMore, highlightMatch, ensuredItems, }: UseComboboxCoreOptions<T>) => UseComboboxCoreReturn<T>
```

### `UseComboboxCoreOptions`

Kind: interface.

Extends: `AsyncComboboxSharedProps<T>`.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `ensuredItems` | no | `T[]` | Always present in the list, whatever the current results are. The selection. |

### `UseComboboxCoreReturn`

Kind: interface.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `strings` | yes | `AsyncComboboxStrings` |  |
| `getKey` | yes | `(item: T) => string` |  |
| `trimmedSearch` | yes | `string` |  |
| `listRef` | yes | `React.RefObject<HTMLDivElement \| null>` |  |
| `baseItems` | yes | `T[]` |  |
| `allItems` | yes | `T[]` | `baseItems` plus the create row, when one is offered. |
| `groupedItems` | yes | `Map<string, T[]> \| null` |  |
| `createOptionItem` | yes | `T \| null` |  |
| `showStatus` | yes | `boolean` |  |
| `showEmpty` | yes | `boolean` |  |
| `showCreateOption` | yes | `boolean` |  |
| `renderItemContent` | yes | `(item: T) => ReactNode` |  |
| `itemToStringLabel` | yes | `(item: T) => string` |  |
| `getItemReactKey` | yes | `(item: T) => string` |  |
| `handleScroll` | yes | `(event: Event) => void` |  |

### `useSuggestions`

Kind: callable.

```text
(config: UseSuggestionsConfig<T>) => UseSuggestionsResult<T>
```

### `UseSuggestionsConfig`

Kind: interface.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `fetchData` | yes | `(query: string, context?: SuggestionsFetchContext) => MaybePromise<T[]>` |  |
| `minQueryLength` | no | `number` | Characters required before a request is issued, not counting surrounding whitespace.<br>Defaults to 1. |
| `debounceMs` | no | `number` | The quiet period that collapses a burst of typing into one request. Defaults to 300. |
| `requestDelay` | no | `number` | A further wait before the fetcher runs, after the loading state is showing (unlike<br>`debounceMs`, which delays deciding to fetch). For rate-limited endpoints. |
| `preload` | no | `boolean` | Fetches the empty query, so there is something before typing. Not debounced. |
| `query` | no | `string` |  |
| `defaultQuery` | no | `string` |  |
| `onQueryChange` | no | `(query: string) => void` |  |
| `value` | no | `T \| null` | `null` is an explicit empty selection; `undefined` means uncontrolled. |
| `defaultValue` | no | `T \| null` |  |
| `onValueChange` | no | `(value: T \| null) => void` |  |
| `open` | no | `boolean` |  |
| `defaultOpen` | no | `boolean` |  |
| `onOpenChange` | no | `(open: boolean) => void` |  |
| `onError` | no | `(error: unknown, query: string) => void` | Aborts never reach this. |

### `UseSuggestionsResult`

Kind: interface.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `items` | yes | `T[]` | The latest answered results. Kept while the next query loads; empty below the threshold. |
| `isLoading` | yes | `boolean` | True from the moment a qualifying query is typed until it is answered — the debounce included. |
| `error` | yes | `unknown \| null` |  |
| `isError` | yes | `boolean` |  |
| `retry` | yes | `() => void` |  |
| `query` | yes | `string` |  |
| `setQuery` | yes | `(value: string) => void` |  |
| `value` | yes | `T \| null` |  |
| `setValue` | yes | `(value: T \| null) => void` |  |
| `open` | yes | `boolean` |  |
| `setOpen` | yes | `(value: boolean) => void` |  |

## Preview recipes

Preview route: Combobox — `/combobox`

### A picker that fetches

```tsx fragment — excerpt from the live preview; surrounding values are supplied by the application
<ResourceCombobox
  fetcher={({ query, limit, signal }) => api.countries.search(query, { limit, signal })}
  getKey={(c) => c.code}
  getLabel={(c) => c.name}
  getDescription={(c) => c.capital}
  getMeta={(c) => c.region}
  highlightMatch
/>
```

### SuggestionsCombobox

```tsx fragment — excerpt from the live preview; surrounding values are supplied by the application
import { SuggestionsCombobox, useSuggestions } from "themelia-ui/features/combobox"

const suggestions = useSuggestions({ fetchData, minQueryLength: 1 })

// or, when it really is a combobox:
<SuggestionsCombobox
  fetchData={(q, { signal }) => api.search(q, { signal })}
  itemKey={(c) => c.code}
  itemText={(c) => c.name}
  preload
/>
```

### When the fetch fails

```tsx fragment — excerpt from the live preview; surrounding values are supplied by the application
<ResourceCombobox
  fetcher={search}
  getKey={(c) => c.code}
  getLabel={(c) => c.name}
  errorMessage="The lookup service is unavailable."
/>
```

### Results you already hold

```tsx fragment — excerpt from the live preview; surrounding values are supplied by the application
<AsyncCombobox
  items={items}
  searchValue={query}
  onSearchValueChange={setQuery}
  selectedValue={selected}
  onSelectedValueChange={setSelected}
  getItemLabel={(c) => c.name}
  getItemKey={(c) => c.code}
  getItemGroup={(c) => c.region}
  highlightMatch
/>
```

### Several at once

```tsx fragment — excerpt from the live preview; surrounding values are supplied by the application
<AsyncMultiCombobox
  items={items}
  selectedValues={selected}
  onSelectedValuesChange={setSelected}
  …
/>
```

### Apply before it counts

```tsx fragment — excerpt from the live preview; surrounding values are supplied by the application
<AsyncMultiCombobox applyButton onApply={run} … />
```

### Create what is missing

```tsx fragment — excerpt from the live preview; surrounding values are supplied by the application
<AsyncCombobox creatable onCreate={(name) => create(name)} … />
```
