<!-- GENERATED by scripts/gen-consumer-docs.mjs — do not edit. -->

# `layout/navigation`

The shell's own navigation — side, section or category — positions destinations.

## Selection

- **Use when:** The shell's own navigation — side, section or category — positions destinations.
- **Avoid when:** The user moves between sibling views inside one page.
- **Consider instead:** `base/navigation`, `layout/sidebar`
- **Stability:** stable.

## Import

```tsx fragment — import-only example
import { BreadcrumbProgress } from "themelia-ui/layout/navigation"
import "themelia-ui/layout/navigation.css"
```

## Composition

This family composes `base/badge`, `base/item`, `base/timeline`, `base/typography`.

Application policy—routing, fetching, persistence, permissions, and translation—stays
outside the package and arrives through the public props, callbacks, slots, or accessors below.

## Public declarations

### `BreadcrumbProgress`

Kind: callable.

```text
({ steps, currentIndex, onStepClick, strings, className, ...props }: BreadcrumbProgressProps) => import("react").JSX.Element
```

### `BreadcrumbProgressProps`

Kind: interface.

Extends: `Omit<ComponentProps<"div">, "onSelect">`.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `steps` | yes | `BreadcrumbProgressStep[]` |  |
| `currentIndex` | yes | `number` | Zero-based. |
| `onStepClick` | no | `(id: string, index: number) => void` | Makes completed steps and the current one reachable, never later ones. Without it the<br>steps are plain list items. |
| `strings` | no | `Partial<BreadcrumbProgressStrings>` |  |

### `BreadcrumbProgressStep`

Kind: interface.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `id` | yes | `string` |  |
| `label` | yes | `ReactNode` |  |
| `hint` | no | `ReactNode` | A second line, shown from lg up where there is room for it. |

### `BreadcrumbProgressStrings`

Kind: interface.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `step` | yes | `(index: number, label: string, status?: "completed" \| "current" \| "upcoming") => string` | Names each step — "Step 2: Billing", and "Step 1: Account, completed" for one already<br>walked past. The current step is marked by `aria-current`, so it needs no word here. |
| `position` | yes | `(current: number, total: number) => string` | The summary above the trail — "Step 2 of 4". |

### `CategoryNav`

Kind: callable.

```text
({ label, items, activeId, onSelect, className, ...props }: CategoryNavProps) => import("react").JSX.Element
```

### `CategoryNavItem`

Kind: interface.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `id` | yes | `string` | Stable identity. The row reports this, not its index. |
| `label` | yes | `ReactNode` |  |
| `icon` | no | `ComponentType<{ className?: string; }>` |  |
| `count` | no | `number` | A count of what the row selects. `0` renders — "none" is information. |
| `hint` | no | `ReactNode` | A second line under the label. |

### `CategoryNavProps`

Kind: interface.

Extends: `Omit<ComponentProps<"nav">, "onSelect" | "title">`.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `label` | no | `string` | Names the region. A page with two rails needs two names. |
| `items` | yes | `CategoryNavItem[]` |  |
| `activeId` | no | `string` |  |
| `onSelect` | no | `(id: string) => void` |  |

### `defaultBreadcrumbProgressStrings`

Kind: const.

```text
BreadcrumbProgressStrings
```

### `defaultSectionNavStrings`

Kind: const.

```text
SectionNavStrings
```

### `defaultSideNavStrings`

Kind: const.

```text
SideNavStrings
```

### `SectionNav`

Kind: callable.

```text
({ items, rootMargin, onSelect, strings, className, ...props }: SectionNavProps) => React.JSX.Element
```

### `SectionNavItem`

Kind: interface.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `id` | yes | `string` | The element id this entry points at. |
| `label` | yes | `React.ReactNode` |  |
| `depth` | no | `number` | Nesting depth. 1 is top level; deeper entries are indented. |

### `SectionNavProps`

Kind: interface.

Extends: `Omit<React.ComponentProps<"nav">, "children" | "onSelect">`.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `items` | yes | `SectionNavItem[]` |  |
| `rootMargin` | no | `string` | Which part of the viewport counts as "here". The default makes a heading current once<br>it reaches the upper quarter. |
| `onSelect` | no | `(id: string) => void` |  |
| `strings` | no | `StringsProp<SectionNavStrings>` |  |

### `SectionNavStrings`

Kind: interface.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `label` | yes | `string` |  |

### `SideNav`

Kind: callable.

```text
({ items, groups, currentPath, renderLink, strings, className, ...props }: SideNavProps) => React.JSX.Element
```

### `SideNavGroup`

Kind: interface.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `id` | yes | `string` | Stable identity, used for the collapsed set. |
| `label` | no | `React.ReactNode` | Caption above the entries. Omit for an uncaptioned block. |
| `items` | yes | `SideNavItem[]` |  |
| `collapsible` | no | `boolean` |  |
| `defaultCollapsed` | no | `boolean` |  |

### `SideNavItem`

Kind: interface.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `label` | yes | `React.ReactNode` |  |
| `href` | yes | `string` | Destination. |
| `icon` | no | `LucideIcon` |  |
| `badge` | no | `React.ReactNode` | A count or a status beside the label. A string or number is wrapped in a neutral<br>`Badge`; pass a node to choose the tone. |
| `disabled` | no | `boolean` |  |

### `SideNavProps`

Kind: interface.

Extends: `Omit<React.ComponentProps<"nav">, "children">`.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `items` | no | `SideNavItem[]` | Flat entries. Mutually exclusive with `groups`. |
| `groups` | no | `SideNavGroup[]` | Captioned blocks of entries. Mutually exclusive with `items`. |
| `currentPath` | no | `string` | The path considered current. Matched by longest prefix, so `/settings/members` marks<br>the members entry, not the `/settings` index. |
| `renderLink` | no | `(props: { href: string; className: string; children: React.ReactNode; "aria-current"?: "page"; "aria-disabled"?: boolean; }) => React.ReactElement` | Renders each entry: receives the link props and returns the router's link element.<br>Without it, entries are plain anchors. |
| `strings` | no | `StringsProp<SideNavStrings>` |  |

### `SideNavStrings`

Kind: interface.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `label` | yes | `string` | Names the region for assistive technology. |

## Preview recipes

Preview route: Side nav & section nav — `/side-nav`

### SideNav

```tsx fragment — excerpt from the live preview; surrounding values are supplied by the application
<SideNav groups={settingsNav} currentPath="/settings/members" />
```

### SectionNav

```tsx fragment — excerpt from the live preview; surrounding values are supplied by the application
<SectionNav
  items={[
    { id: "side-nav", label: "SideNav" },
    { id: "section-nav", label: "SectionNav", depth: 2 },
  ]}
/>
```

### One component, named for its shape

```tsx fragment — excerpt from the live preview; surrounding values are supplied by the application
<BreadcrumbProgress
  steps={[{ id: "account", label: "Account" }, { id: "billing", label: "Billing" }, …]}
  currentIndex={current}
  onStepClick={(_id, index) => setCurrent(index)}
/>
```
