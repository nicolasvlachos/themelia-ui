<!-- GENERATED by scripts/gen-consumer-docs.mjs — do not edit. -->

# `layout/settings`

Settings pages share an aside navigation beside their content.

## Selection

- **Use when:** Settings pages share an aside navigation beside their content.
- **Avoid when:** The navigation is the application sidebar, or the sections are tabs.
- **Consider instead:** `layout/navigation`, `base/navigation`
- **Stability:** stable.

## Import

```tsx fragment — import-only example
import { AsideNavShell } from "themelia-ui/layout/settings"
import "themelia-ui/layout/settings.css"
```

## Composition

This family composes `base/navigation`, `layout/containers`, `layout/navigation`.

Application policy—routing, fetching, persistence, permissions, and translation—stays
outside the package and arrives through the public props, callbacks, slots, or accessors below.

## Public declarations

### `AsideNavGroup`

Kind: interface.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `id` | yes | `string` | Stable identity, used for the collapsed set. |
| `label` | no | `React.ReactNode` | Caption above the entries. Omit for an uncaptioned block. |
| `items` | yes | `SideNavItem[]` |  |
| `collapsible` | no | `boolean` |  |
| `defaultCollapsed` | no | `boolean` |  |

### `AsideNavItem`

Kind: interface.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `label` | yes | `React.ReactNode` |  |
| `href` | yes | `string` | Destination. |
| `icon` | no | `LucideIcon` |  |
| `badge` | no | `React.ReactNode` | A count or a status beside the label. A string or number is wrapped in a neutral<br>`Badge`; pass a node to choose the tone. |
| `disabled` | no | `boolean` |  |

### `AsideNavShell`

Kind: callable.

```text
({ title, description, items, groups, currentPath, aside, renderLink, actions, stickyAside, className, children, ...props }: AsideNavShellProps) => React.JSX.Element
```

### `AsideNavShellProps`

Kind: interface.

Extends: `Omit<React.ComponentProps<"div">, "title">`.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `title` | no | `React.ReactNode` |  |
| `description` | no | `React.ReactNode` |  |
| `items` | no | `SideNavItem[]` | Flat entries, or captioned groups. Passed straight to SideNav. |
| `groups` | no | `SideNavGroup[]` |  |
| `currentPath` | no | `string` | The path considered current. Matched by longest prefix. |
| `aside` | no | `React.ReactNode` | Replaces the built-in SideNav entirely. |
| `renderLink` | no | `React.ComponentProps<typeof SideNav>["renderLink"]` |  |
| `actions` | no | `React.ReactNode` | Actions for the heading row. |
| `stickyAside` | no | `boolean` |  |

## Preview recipes

Preview route: Side nav & section nav — `/side-nav`

### The rail beside its content: AsideNavShell

```tsx fragment — excerpt from the live preview; surrounding values are supplied by the application
<AsideNavShell title="Settings" groups={settingsNav} currentPath="/settings/members">
  <Card title="Members" />
</AsideNavShell>
```
