<!-- GENERATED by scripts/gen-consumer-docs.mjs — do not edit. -->

# `layout/containers`

A page region needs width constraint, a titled section, or a two-column split.

## Selection

- **Use when:** A page region needs width constraint, a titled section, or a two-column split.
- **Avoid when:** The children are peers needing only spacing, or the region is the app shell.
- **Consider instead:** `base/structure`, `layout/workspace`
- **Stability:** stable.

## Import

```tsx fragment — import-only example
import { Container } from "themelia-ui/layout/containers"
import "themelia-ui/layout/containers.css"
```

## Composition

This family has no component-family dependencies.

Application policy—routing, fetching, persistence, permissions, and translation—stays
outside the package and arrives through the public props, callbacks, slots, or accessors below.

## Public declarations

### `Container`

Kind: const.

```text
React.ForwardRefExoticComponent<Omit<ContainerProps, "ref"> & React.RefAttributes<HTMLDivElement>>
```

### `ContainerGutter`

Kind: type.

```text
"none" | "sm" | "md" | "lg"
```

### `ContainerMaxWidth`

Kind: type.

```text
"sm" | "md" | "lg" | "xl" | "2xl" | "full"
```

### `ContainerProps`

Kind: interface.

Extends: `React.ComponentProps<"div">`.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `maxWidth` | no | `ContainerMaxWidth` | The reading measure. Defaults to `xl`. |
| `gutter` | no | `ContainerGutter` | The inline gutter. Defaults to `md`. |

### `PageViewport`

Kind: const.

```text
React.ForwardRefExoticComponent<Omit<PageViewportProps, "ref"> & React.RefAttributes<HTMLDivElement>>
```

### `PageViewportProps`

Kind: interface.

Extends: `React.ComponentProps<"div">`.

No own members are present in the normalized public snapshot.

### `Section`

Kind: const.

```text
React.ForwardRefExoticComponent<Omit<SectionProps, "ref"> & React.RefAttributes<HTMLElement>>
```

### `SectionProps`

Kind: interface.

Extends: `React.ComponentProps<"section">`.

No own members are present in the normalized public snapshot.

### `TwoColumnLayout`

Kind: const.

```text
React.ForwardRefExoticComponent<Omit<TwoColumnLayoutProps, "ref"> & React.RefAttributes<HTMLDivElement>>
```

### `TwoColumnLayoutProps`

Kind: interface.

Extends: `Omit<React.ComponentProps<"div">, "children">`.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `header` | no | `React.ReactNode` | Spans both columns, before the work regions. |
| `main` | yes | `React.ReactNode` | Primary detail, form, or index content. |
| `aside` | yes | `React.ReactNode` | Secondary summary, support, or action rail. |
| `footer` | no | `React.ReactNode` | Spans both columns, after the work regions. |
| `stickyAside` | no | `boolean` | Keeps the aside in view while the page scrolls. Opt-in: an aside taller than the<br>viewport must be able to scroll away. |
| `asidePosition` | no | `"start" \| "end"` | Which side the aside is drawn on. Default `end`. Only the grid columns move; the<br>aside stays second in the DOM. |

## Preview recipes

Preview route: Containers — `/containers`

### The four blocks

```tsx fragment — excerpt from the live preview; surrounding values are supplied by the application
<PageViewport>
  <Container maxWidth="xl" gutter="md">
    <Section>
      <PageHeading title="Invoices" />
      <Card>…</Card>
    </Section>
  </Container>
</PageViewport>
```

### TwoColumnLayout

```tsx fragment — excerpt from the live preview; surrounding values are supplied by the application
<TwoColumnLayout
  main={<InvoiceForm />}
  aside={<SummaryCard />}
  asidePosition="end"
  stickyAside
/>
```
