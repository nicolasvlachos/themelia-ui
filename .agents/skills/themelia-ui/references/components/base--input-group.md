<!-- GENERATED by scripts/gen-consumer-docs.mjs — do not edit. -->

# `base/input-group`

A control needs an affix inside its border — an icon, unit, prefix button or counter.

## Selection

- **Use when:** A control needs an affix inside its border — an icon, unit, prefix button or counter.
- **Avoid when:** The affix is a separate field, or the label belongs above the control.
- **Consider instead:** `base/text-inputs`, `base/forms`
- **Stability:** stable.

## Import

```tsx fragment — import-only example
import { InputGroup } from "themelia-ui/base/input-group"
import "themelia-ui/base/input-group.css"
```

## Composition

This family composes `base/buttons`, `base/text-inputs`, `base/typography`.

Application policy—routing, fetching, persistence, permissions, and translation—stays
outside the package and arrives through the public props, callbacks, slots, or accessors below.

## Public declarations

### `InputGroup`

Kind: callable.

```text
({ className, id, "aria-labelledby": labelledBy, "aria-describedby": describedBy, "aria-invalid": invalid, "aria-required": required, ...props }: React.ComponentProps<"div">) => React.JSX.Element
```

### `InputGroupAddon`

Kind: callable.

```text
({ className, align, ...props }: React.ComponentProps<"div"> & VariantProps<typeof inputGroupAddonVariants>) => React.JSX.Element
```

### `InputGroupAddonProps`

Kind: type.

```text
React.ComponentProps<"div">
```

### `InputGroupButton`

Kind: callable.

```text
({ className, type, tone, buttonStyle, size, ...props }: Omit<React.ComponentProps<typeof Button>, "size" | "type"> & VariantProps<typeof inputGroupButtonVariants> & { type?: "button" | "submit" | "reset"; }) => React.JSX.Element
```

### `InputGroupButtonProps`

Kind: type.

```text
React.ComponentProps<typeof Button>
```

### `InputGroupInput`

Kind: callable.

```text
({ className, ...props }: React.ComponentProps<"input">) => React.JSX.Element
```

### `InputGroupInputProps`

Kind: type.

```text
React.ComponentProps<"input">
```

### `InputGroupProps`

Kind: type.

```text
React.ComponentProps<"div">
```

### `InputGroupText`

Kind: callable.

```text
({ className, ...props }: React.ComponentProps<"span">) => React.JSX.Element
```

### `InputGroupTextarea`

Kind: callable.

```text
({ className, ...props }: TextareaProps) => React.JSX.Element
```

### `InputGroupTextareaProps`

Kind: type.

```text
React.ComponentProps<"textarea">
```

### `InputGroupTextProps`

Kind: type.

```text
React.ComponentProps<"span">
```

## Preview recipes

Preview route: Input group — `/input-group`

### Attached along the line

```tsx fragment — excerpt from the live preview; surrounding values are supplied by the application
<InputGroup>
  <InputGroupAddon><SearchIcon /></InputGroupAddon>
  <InputGroupInput placeholder="Search orders" />
</InputGroup>
```

### Attached above or below

```tsx fragment — excerpt from the live preview; surrounding values are supplied by the application
<InputGroup>
  <InputGroupTextarea placeholder="Write a note" />
  <InputGroupAddon align="block-end">
    <InputGroupText>Markdown supported</InputGroupText>
  </InputGroupAddon>
</InputGroup>
```

### Button sizes inside the shell

```tsx fragment — excerpt from the live preview; surrounding values are supplied by the application
<InputGroupButton size="icon-xs" aria-label="Go"><ArrowRightIcon /></InputGroupButton>
```
