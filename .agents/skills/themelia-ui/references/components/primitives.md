<!-- GENERATED by scripts/gen-consumer-docs.mjs — do not edit. -->

# `primitives`

Displaying or formatting exactly one read-only value — currency/money, date, email, file size, initials — with no interaction.

## Selection

- **Use when:** Displaying or formatting exactly one read-only value — currency/money, date, email, file size, initials — with no interaction.
- **Avoid when:** The value is editable, or the text is prose in a typographic role.
- **Consider instead:** `base/typography`, `base/text-inputs`
- **Stability:** stable.

## Import

```tsx fragment — import-only example
import { Address } from "themelia-ui/primitives"
import "themelia-ui/primitives.css"
```

## Composition

This family composes `base/typography`.

Application policy—routing, fetching, persistence, permissions, and translation—stays
outside the package and arrives through the public props, callbacks, slots, or accessors below.

## Public declarations

### `Address`

Kind: callable.

```text
({ value, format, countryCode, order, emptyLabel, size, weight, type, className, ref, ...props }: AddressProps) => import("react").JSX.Element
```

### `AddressField`

Kind: type.

```text
keyof AddressParts
```

### `AddressOrder`

Kind: type.

```text
readonly AddressLine[]
```

### `AddressParts`

Kind: interface.

Postal address ordering. There is no `Intl` for addresses, so this ships three common
orderings keyed by country, a documented default, and an explicit `order` for the rest.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `line1` | no | `string \| null` |  |
| `line2` | no | `string \| null` |  |
| `city` | no | `string \| null` |  |
| `region` | no | `string \| null` | State, province, county — whatever the country calls the level above a city. |
| `postalCode` | no | `string \| null` |  |
| `country` | no | `string \| null` |  |

### `AddressProps`

Kind: interface.

Extends: `Omit<ComponentPropsWithoutRef<"address">, "children" | "color">`.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `value` | no | `AddressParts \| null` | The address, as fields. Anything empty simply drops out. |
| `format` | no | `"block" \| "inline"` | `block` is the envelope form across several lines; `inline` is one line for a cell. |
| `countryCode` | no | `string \| null` | An ISO country code deciding the line order (`US`, `GB`, `DE`). Read from<br>`value.country` when that is a code; never inferred from the reader's locale. |
| `order` | no | `AddressOrder` | Overrides the ordering outright, for a country the kit does not know. |
| `emptyLabel` | no | `ReactNode` |  |
| `size` | no | `ValueProps["size"]` |  |
| `weight` | no | `ValueProps["weight"]` |  |
| `type` | no | `ValueProps["type"]` |  |
| `ref` | no | `Ref<HTMLElement>` |  |

### `Coordinates`

Kind: callable.

```text
({ latitude, longitude, format, precision, showHemisphere, locale, strings, align, ...props }: CoordinatesProps) => import("react").JSX.Element
```

### `CoordinatesProps`

Kind: interface.

Extends: `SpanProps`.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `latitude` | no | `number \| null` |  |
| `longitude` | no | `number \| null` |  |
| `format` | no | `"decimal" \| "dms"` | `decimal` is "48.85837, 2.29448"; `dms` is "48°51'30.1\"N 2°17'40.1\"E". |
| `precision` | no | `number` | Decimal places in `decimal` form. Five is about a metre. |
| `showHemisphere` | no | `boolean` | Adds hemisphere letters to the decimal form: "48.85837°N, 2.29448°E". Off by default<br>(a signed pair pastes into a map); `dms` always shows them. |
| `locale` | no | `string` |  |
| `strings` | no | `Partial<CoordinatesStrings>` |  |
| `emptyLabel` | no | `ReactNode` |  |
| `size` | no | `ValueProps["size"]` |  |
| `align` | no | `ValueProps["align"]` |  |
| `weight` | no | `ValueProps["weight"]` |  |
| `type` | no | `ValueProps["type"]` |  |
| `ref` | no | `Ref<HTMLSpanElement>` |  |

### `CoordinatesStrings`

Kind: interface.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `north` | yes | `string` |  |
| `south` | yes | `string` |  |
| `east` | yes | `string` |  |
| `west` | yes | `string` |  |

### `Date`

Kind: callable.

```text
({ value, pattern, ...props }: DateBaseProps) => import("react").JSX.Element
```

### `DateBaseProps`

Kind: interface.

What DatePrimitive, Time, and DateTime all take. Three components, one shape.

Extends: `SpanProps`.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `value` | no | `DateInput` |  |
| `pattern` | no | `string` | date-fns pattern. Falls back to the scope's `dateFormat`. |
| `emptyLabel` | no | `ReactNode` |  |
| `size` | no | `ValueProps["size"]` |  |
| `align` | no | `ValueProps["align"]` |  |
| `weight` | no | `ValueProps["weight"]` |  |
| `type` | no | `ValueProps["type"]` |  |
| `ref` | no | `Ref<HTMLSpanElement>` |  |

### `DateInput`

Kind: type.

```text
Date | string | number | null | undefined
```

### `DatePrimitive`

Kind: callable.

```text
({ value, pattern, ...props }: DateBaseProps) => import("react").JSX.Element
```

### `DateRange`

Kind: callable.

```text
({ start, end, pattern, separator, ...props }: DateRangeProps) => import("react").JSX.Element
```

### `DateRangeProps`

Kind: interface.

Extends: `Omit<DateBaseProps, "value">`.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `start` | no | `DateInput` |  |
| `end` | no | `DateInput` |  |
| `separator` | no | `string` |  |

### `DateTime`

Kind: callable.

```text
({ value, pattern, ...props }: DateBaseProps) => import("react").JSX.Element
```

### `DEFAULT_ADDRESS_ORDER`

Kind: const.

```text
AddressOrder
```

### `defaultCoordinatesStrings`

Kind: const.

```text
CoordinatesStrings
```

### `defaultInlineListStrings`

Kind: const.

```text
InlineListStrings
```

### `defaultRatingStrings`

Kind: const.

```text
RatingStrings
```

### `defaultRatioStrings`

Kind: const.

```text
RatioStrings
```

### `DimensionPart`

Kind: type.

```text
number | string | null | undefined
```

### `Dimensions`

Kind: callable.

```text
({ width, height, depth, unit, separator, locale, options, ...props }: DimensionsProps) => import("react").JSX.Element
```

### `DimensionsProps`

Kind: interface.

Extends: `SpanProps`.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `width` | no | `DimensionPart` |  |
| `height` | no | `DimensionPart` |  |
| `depth` | no | `DimensionPart` |  |
| `unit` | no | `ReactNode` |  |
| `separator` | no | `ReactNode` |  |
| `locale` | no | `string` |  |
| `options` | no | `Intl.NumberFormatOptions` |  |
| `emptyLabel` | no | `ReactNode` |  |
| `size` | no | `ValueProps["size"]` |  |
| `align` | no | `ValueProps["align"]` |  |
| `weight` | no | `ValueProps["weight"]` |  |
| `type` | no | `ValueProps["type"]` |  |
| `ref` | no | `Ref<HTMLSpanElement>` |  |

### `Duration`

Kind: callable.

```text
({ value, from, locale, maxParts, unitDisplay, minimumFractionDigits, maximumFractionDigits, ...props }: DurationProps) => import("react").JSX.Element
```

### `DurationProps`

Kind: interface.

Extends: `SpanProps`.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `value` | no | `number \| null` |  |
| `from` | no | `DurationUnit` | The unit the incoming value is in. |
| `locale` | no | `string` |  |
| `maxParts` | no | `number` | How many units to show before truncating. |
| `unitDisplay` | no | `DurationUnitDisplay` |  |
| `minimumFractionDigits` | no | `number` |  |
| `maximumFractionDigits` | no | `number` |  |
| `emptyLabel` | no | `ReactNode` |  |
| `size` | no | `ValueProps["size"]` |  |
| `align` | no | `ValueProps["align"]` |  |
| `weight` | no | `ValueProps["weight"]` |  |
| `type` | no | `ValueProps["type"]` |  |
| `ref` | no | `Ref<HTMLSpanElement>` |  |

### `DurationUnit`

Kind: type.

Options for localized duration formatting. Defaults: seconds in, narrow units out.

```text
'milliseconds' | 'seconds' | 'minutes' | 'hours' | 'days'
```

### `DurationUnitDisplay`

Kind: type.

```text
'narrow' | 'short' | 'long'
```

### `Email`

Kind: callable.

```text
({ value, display, subject, body, ...props }: EmailProps) => import("react").JSX.Element
```

### `EmailProps`

Kind: interface.

Extends: `Omit<LinkProps, "href" | "children" | "ref">`.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `value` | no | `string \| null` |  |
| `display` | no | `ReactNode` | Shown instead of the address — a person's name, for instance. |
| `subject` | no | `string` |  |
| `body` | no | `string` |  |
| `ref` | no | `Ref<HTMLAnchorElement>` |  |

### `EmptyValue`

Kind: callable.

The absent state on its own, for a slot that has nothing to show.

```text
({ label, ...props }: EmptyValueProps) => import("react").JSX.Element
```

### `EmptyValueProps`

Kind: interface.

Extends: `Omit<ValueProps, "children">`.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `label` | no | `ReactNode` |  |

### `FileSize`

Kind: callable.

```text
({ value, from, base, locale, align, ...props }: FileSizeProps) => import("react").JSX.Element
```

### `FileSizeBase`

Kind: type.

```text
"binary" | "decimal" | "iec"
```

### `FileSizeFormatOptions`

Kind: interface.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `from` | no | `FileSizeUnit` | The unit the incoming value is already in. |
| `base` | no | `FileSizeBase` | Which base and labels to use (see the note above). Default `binary`. |
| `locale` | no | `string` |  |

### `FileSizeProps`

Kind: interface.

Extends: `SpanProps`.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `value` | no | `number \| null` |  |
| `from` | no | `FileSizeUnit` |  |
| `base` | no | `FileSizeBase` | `binary` (default), `decimal`, or `iec`. See file-size.format.ts. |
| `locale` | no | `string` |  |
| `emptyLabel` | no | `ReactNode` |  |
| `size` | no | `ValueProps["size"]` |  |
| `align` | no | `ValueProps["align"]` |  |
| `weight` | no | `ValueProps["weight"]` |  |
| `type` | no | `ValueProps["type"]` |  |
| `ref` | no | `Ref<HTMLSpanElement>` |  |

### `FileSizeUnit`

Kind: type.

```text
"bytes" | "kilobytes" | "megabytes" | "gigabytes"
```

### `formatAddress`

Kind: callable.

The same address on one line, for a cell or a summary row.

```text
(parts: AddressParts, options?: FormatAddressOptions) => string
```

### `formatAddressLines`

Kind: callable.

Returns the address as lines, each already joined. Empty fields drop out, and a line
left with nothing drops with them, so a missing `line2` never leaves a blank row.

```text
(parts: AddressParts, { countryCode, order, separator, locale }?: FormatAddressOptions) => string[]
```

### `FormatAddressOptions`

Kind: interface.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `countryCode` | no | `string \| null` | An ISO country code deciding the line order, matched case-insensitively. Falls back<br>to `DEFAULT_ADDRESS_ORDER`; the reader's locale is never used to guess it. |
| `order` | no | `AddressOrder` | Overrides the ordering outright. |
| `separator` | no | `string` | Joins fields on a line that does not specify its own. |
| `locale` | no | `string` | The locale a two-letter `country` is named in ("GB" becomes "United Kingdom") via<br>`Intl.DisplayNames`; English when omitted. A longer `country` is printed as given. |

### `formatDateRange`

Kind: callable.

Collapses a range to its distinct parts: same day renders once, and a range within one
month drops the repeated month from the start date.

```text
(start: DateInput, end: DateInput, { pattern, separator, locale }?: FormatDateRangeOptions) => string | undefined
```

### `FormatDateRangeOptions`

Kind: interface.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `pattern` | no | `string` |  |
| `separator` | no | `string` |  |
| `locale` | no | `Locale` | date-fns locale object. The React component reads it from the scope for you. |

### `formatDimensions`

Kind: callable.

```text
(width: DimensionPart, height: DimensionPart, { depth, unit, separator, locale, options }?: FormatDimensionsOptions) => string | undefined
```

### `FormatDimensionsOptions`

Kind: interface.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `depth` | no | `DimensionPart` |  |
| `unit` | no | `string` |  |
| `separator` | no | `string` |  |
| `locale` | no | `string` |  |
| `options` | no | `Intl.NumberFormatOptions` |  |

### `FormatDurationOptions`

Kind: interface.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `from` | no | `DurationUnit` | Unit used by the incoming value. Defaults to seconds. |
| `locale` | no | `string` | BCP-47 locale passed to Intl.NumberFormat. |
| `maxParts` | no | `number` | Number of non-zero units to show. Defaults to all five units. |
| `unitDisplay` | no | `DurationUnitDisplay` | Intl unit-label style. `narrow` provides the compact `1h 5m` default. |
| `minimumFractionDigits` | no | `number` |  |
| `maximumFractionDigits` | no | `number` |  |

### `formatFileSize`

Kind: callable.

```text
(value: number, { from, base, locale }?: FileSizeFormatOptions) => string
```

### `formatInitials`

Kind: callable.

Derive short, Unicode-aware initials from a person or resource name.

`first-last` is the identity default (`Mary Jane Smith` → `MS`). Use
`first-words` for admin surfaces that intentionally use the first
two words (`Mary Jane Smith` → `MJ`). Single-word `first-last` values use
the first N letters (`OpenAI` → `OP`).

```text
(value: string | null | undefined, options?: InitialsFormatOptions) => string
```

### `formatName`

Kind: callable.

```text
(value: string, { force }?: NameFormatOptions) => string
```

### `Initials`

Kind: callable.

```text
({ value, strategy, maxCharacters, locale, fallback, ...props }: InitialsProps) => import("react").JSX.Element
```

### `InitialsFormatOptions`

Kind: interface.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `strategy` | no | `InitialsStrategy` | First + last word by default; use first-words to preserve the first N words. |
| `maxCharacters` | no | `InitialsMaxCharacters` | Maximum visible characters. Defaults to 2. |
| `locale` | no | `string` | Optional BCP-47 locale used for locale-sensitive uppercasing. |
| `fallback` | no | `string` | Returned when no usable letter or number exists. Defaults to an empty string. |

### `InitialsMaxCharacters`

Kind: type.

```text
1 | 2 | 3
```

### `InitialsProps`

Kind: interface.

Extends: `Omit<ValueProps, "children">`.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `value` | no | `string \| null` | Person, organisation, or resource name to derive initials from. |
| `strategy` | no | `InitialsStrategy` | First + last word by default; `first-words` keeps the leading N words instead. |
| `maxCharacters` | no | `InitialsMaxCharacters` |  |
| `locale` | no | `string` |  |
| `fallback` | no | `string` | Returned when no usable letter or number exists. |
| `ref` | no | `Ref<HTMLSpanElement>` |  |

### `InitialsStrategy`

Kind: type.

```text
'first-last' | 'first-words'
```

### `InlineList`

Kind: callable.

```text
({ items, join, joinStyle, max, locale, strings, align, ...props }: InlineListProps) => import("react").JSX.Element
```

### `InlineListProps`

Kind: interface.

Extends: `SpanProps`.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `items` | no | `readonly string[] \| null` |  |
| `join` | no | `"and" \| "or" \| "none"` | `and` is "A, B and C"; `or` is "A, B or C"; `none` is "A, B, C", for lists that are<br>not prose. (Not `type`, which primitives use for the text tone.) |
| `joinStyle` | no | `"long" \| "short" \| "narrow"` | `long` is "and", `short` is "&" where the locale has one, `narrow` drops it.<br>(Not `style`, the DOM attribute.) |
| `max` | no | `number` | Shows at most this many, then a count of the rest inside the list, so the<br>conjunction still lands correctly: "Alice, Bob and 3 more". |
| `locale` | no | `string` |  |
| `strings` | no | `Partial<InlineListStrings>` |  |
| `emptyLabel` | no | `ReactNode` |  |
| `size` | no | `ValueProps["size"]` |  |
| `align` | no | `ValueProps["align"]` |  |
| `weight` | no | `ValueProps["weight"]` |  |
| `type` | no | `ValueProps["type"]` |  |
| `ref` | no | `Ref<HTMLSpanElement>` |  |

### `InlineListStrings`

Kind: interface.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `more` | yes | `(count: number) => string` | Names the items not shown when `max` truncates. Receives the count. |

### `Link`

Kind: callable.

```text
({ children, emptyLabel, className, ref, ...props }: LinkProps) => import("react").JSX.Element
```

### `LinkProps`

Kind: interface.

Extends: `AnchorHTMLAttributes<HTMLAnchorElement>`.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `children` | no | `ReactNode` |  |
| `emptyLabel` | no | `ReactNode` |  |
| `ref` | no | `Ref<HTMLAnchorElement>` |  |

### `Measure`

Kind: callable.

```text
({ value, unit, unitDisplay, maximumFractionDigits, locale, align, ...props }: MeasureProps) => import("react").JSX.Element
```

### `MeasureProps`

Kind: interface.

Extends: `SharedProps`.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `value` | no | `number \| null` |  |
| `unit` | no | `string` | A CSS-style unit identifier — `kilogram`, `meter`, `liter`, `celsius`, `byte`.<br><br>Passed to `Intl.NumberFormat`, which owns both the abbreviation and where it goes:<br>English writes "2.5 kg" and French writes "2,5 kg", and neither is a string this<br>component should be assembling. |
| `unitDisplay` | no | `"short" \| "narrow" \| "long"` | `short` is "2.5 kg", `narrow` is "2.5kg", `long` is "2.5 kilograms". |
| `maximumFractionDigits` | no | `number` | Caps the decimals. The value is not rounded before formatting. |

### `Money`

Kind: callable.

```text
({ amount, currency, unit, minorUnitScale, formatMode, locale, align, secondary, displayMode, layout, secondaryEmphasis, separator, className, ...props }: MoneyProps) => import("react").JSX.Element
```

### `MoneyProps`

Kind: interface.

Extends: `SpanProps`, `MoneyValue`.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `locale` | no | `string` |  |
| `emptyLabel` | no | `ReactNode` |  |
| `size` | no | `ValueProps["size"]` |  |
| `align` | no | `ValueProps["align"]` |  |
| `weight` | no | `ValueProps["weight"]` |  |
| `type` | no | `ValueProps["type"]` |  |
| `secondary` | no | `MoneyValue \| null` | A converted value shown beside the primary one. The scope's policy can hide it<br>(`displayMode="primary-only"`) but never invents one. |
| `displayMode` | no | `MoneyDisplayMode` |  |
| `layout` | no | `MoneyLayout` |  |
| `secondaryEmphasis` | no | `MoneySecondaryEmphasis` |  |
| `separator` | no | `ReactNode` | Between the two, inline. A middle dot; pass an arrow for a conversion. |
| `ref` | no | `Ref<HTMLSpanElement>` |  |

### `MoneyUnit`

Kind: type.

The unit the amount ARRIVES in. `minor` is cents, pence, öre.

```text
"major" | "minor"
```

### `MoneyValue`

Kind: interface.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `amount` | no | `number \| string \| null` | A number or decimal string. A single dot is decimal; grouped US/EU strings are also accepted. |
| `currency` | no | `string` |  |
| `unit` | no | `MoneyUnit` | Defaults to `major`. Use `minor` for APIs that store integer minor units. |
| `minorUnitScale` | no | `number` | Minor units per major unit. Defaults to 100. |
| `formatMode` | no | `MoneyFormatMode` | How this amount is written, overriding the scope's `formatMode`. |

### `MonoValue`

Kind: callable.

Identifiers, hashes, SKUs — fixed width so they compare down a column.

```text
({ className, ...props }: ValueProps) => import("react").JSX.Element
```

### `MutedValue`

Kind: callable.

Incidental copy — present, but not something the reader is meant to act on.

```text
({ type, size, ...props }: ValueProps) => import("react").JSX.Element
```

### `Name`

Kind: callable.

```text
({ value, children, force, ...props }: NameProps) => import("react").JSX.Element
```

### `NameFormatOptions`

Kind: interface.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `force` | no | `boolean` | Title-case even when the value already looks intentionally cased. |

### `NameProps`

Kind: interface.

Extends: `Omit<ValueProps, "children">`.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `value` | no | `string \| null` |  |
| `children` | no | `ReactNode` |  |
| `force` | no | `boolean` |  |

### `Number`

Kind: callable.

```text
({ value, locale, options, align, ...props }: NumberProps) => import("react").JSX.Element
```

### `NumberProps`

Kind: interface.

Extends: `SpanProps`.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `value` | no | `number \| string \| null` |  |
| `locale` | no | `string` |  |
| `options` | no | `Intl.NumberFormatOptions` |  |
| `emptyLabel` | no | `ReactNode` |  |
| `size` | no | `ValueProps["size"]` |  |
| `align` | no | `ValueProps["align"]` |  |
| `weight` | no | `ValueProps["weight"]` |  |
| `type` | no | `ValueProps["type"]` |  |
| `ref` | no | `Ref<HTMLSpanElement>` |  |

### `parseDateInput`

Kind: callable.

Returns undefined rather than an Invalid Date, so the empty state renders instead.

```text
(value: DateInput) => Date | undefined
```

### `Percent`

Kind: callable.

```text
({ value, locale, options, scaled, align, ...props }: PercentProps) => import("react").JSX.Element
```

### `PercentProps`

Kind: interface.

Extends: `Omit<NumberProps, "options">`.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `scaled` | no | `boolean` | The value is already 0–100 rather than 0–1. |
| `options` | no | `Intl.NumberFormatOptions` |  |

### `Phone`

Kind: callable.

```text
({ value, display, ...props }: PhoneProps) => import("react").JSX.Element
```

### `PhoneProps`

Kind: interface.

Extends: `Omit<LinkProps, "href" | "children" | "ref">`.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `value` | no | `string \| null` |  |
| `display` | no | `ReactNode` |  |
| `ref` | no | `Ref<HTMLAnchorElement>` |  |

### `PluralForms`

Kind: interface.

The noun forms a locale might ask for. `one` and `other` cover English; the rest are the
other `Intl.PluralRules` categories. Anything missing falls back to `other`.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `one` | yes | `string` |  |
| `other` | yes | `string` |  |
| `zero` | no | `string` |  |
| `two` | no | `string` |  |
| `few` | no | `string` |  |
| `many` | no | `string` |  |

### `Quantity`

Kind: callable.

```text
({ value, unit, zeroLabel, locale, align, ...props }: QuantityProps) => import("react").JSX.Element
```

### `QuantityProps`

Kind: interface.

Extends: `SharedProps`.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `value` | no | `number \| null` |  |
| `unit` | no | `PluralForms \| string` | The noun, in the forms the locale may need. A bare string is used for every form. |
| `zeroLabel` | no | `ReactNode` | Rendered instead when the count is zero ("no items"). Off by default. |

### `Range`

Kind: callable.

```text
({ from, to, currency, unit, maximumFractionDigits, locale, align, ...props }: RangeProps) => import("react").JSX.Element
```

### `RangeProps`

Kind: interface.

Extends: `SpanProps`.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `from` | no | `number \| null` |  |
| `to` | no | `number \| null` |  |
| `currency` | no | `string` | An ISO currency code. Renders the range as money. |
| `unit` | no | `string` | A CSS-style unit identifier — `kilogram`, `day`. Renders the range with its unit. |
| `maximumFractionDigits` | no | `number` |  |
| `locale` | no | `string` |  |
| `emptyLabel` | no | `ReactNode` |  |
| `size` | no | `ValueProps["size"]` |  |
| `align` | no | `ValueProps["align"]` |  |
| `weight` | no | `ValueProps["weight"]` |  |
| `type` | no | `ValueProps["type"]` |  |
| `ref` | no | `Ref<HTMLSpanElement>` |  |

### `Rating`

Kind: callable.

```text
({ value, max, hideMax, locale, strings, align, ...props }: RatingProps) => import("react").JSX.Element
```

### `RatingProps`

Kind: interface.

Extends: `SharedProps`.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `value` | no | `number \| null` |  |
| `max` | no | `number` | The top of the scale. Five unless said otherwise, which is the common instrument. |
| `hideMax` | no | `boolean` | Hides the scale, leaving the score alone.<br><br>For a surface that states the scale elsewhere — a column headed "Rating / 5", or a<br>row of stars that already shows it. Not the default, because "4.5" on its own is a<br>number a reader has to guess the meaning of. |
| `strings` | no | `Partial<RatingStrings>` |  |

### `RatingStrings`

Kind: interface.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `outOf` | yes | `string` | Joins the score to the scale. "4.5 out of 5". |

### `Ratio`

Kind: callable.

```text
({ value, total, format, locale, strings, align, ...props }: RatioProps) => import("react").JSX.Element
```

### `RatioProps`

Kind: interface.

Extends: `SharedProps`.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `value` | no | `number \| null` |  |
| `total` | no | `number \| null` | What the value is counted against. Without it the component renders the value alone. |
| `format` | no | `"words" \| "fraction"` | `words` reads "3 of 10" (prose); `fraction` reads "3/10" (tables). |
| `strings` | no | `Partial<RatioStrings>` |  |

### `RatioStrings`

Kind: interface.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `of` | yes | `string` | Joins the two numbers. "3 of 10". |
| `fraction` | yes | `string` | The separator for `format="fraction"`. |

### `RelativeTime`

Kind: callable.

"3 days ago" — for recency, where the exact timestamp is not the point. The precise
value stays available in the title attribute.

```text
({ value, now, addSuffix, includeSeconds, formatRelativeTime, ...props }: RelativeTimeProps) => import("react").JSX.Element
```

### `RelativeTimeProps`

Kind: interface.

Extends: `Omit<DateBaseProps, "pattern">`.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `now` | no | `DateInput` | The moment to measure against. Defaults to the clock.<br><br>Supply it wherever the render has to be reproducible — a server render whose<br>markup must match the client's, a test asserting the string, a visual snapshot.<br>Reading the clock inside the component makes all three of those flaky, and it<br>does not buy a live value in exchange: nothing re-renders it as time passes. |
| `addSuffix` | no | `boolean` | "7 days ago" rather than "7 days". On by default, because a bare duration beside a<br>row of dates reads as a length rather than a moment. |
| `includeSeconds` | no | `boolean` | Distinguishes "less than a minute" from "30 seconds". |
| `formatRelativeTime` | no | `(date: Date, now: Date) => string` | Replaces the wording for this one value. Falls back to the scope's<br>`dates.formatRelativeTime`, and then to date-fns. |

### `SecondaryValue`

Kind: callable.

A value that supports the primary one — a subtitle, a unit, a qualifier.

```text
({ type, ...props }: ValueProps) => import("react").JSX.Element
```

### `SpanProps`

Kind: type.

```text
Omit<ComponentPropsWithoutRef<"span">, "children" | "color">
```

### `Time`

Kind: callable.

```text
({ value, pattern, ...props }: DateBaseProps) => import("react").JSX.Element
```

### `Url`

Kind: callable.

```text
({ value, display, external, strings, ...props }: UrlProps) => import("react").JSX.Element
```

### `UrlProps`

Kind: interface.

Extends: `Omit<LinkProps, "href" | "children" | "ref">`.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `value` | no | `string \| null` |  |
| `display` | no | `ReactNode` |  |
| `external` | no | `boolean` | Opens in a new tab, with the rel hardening that requires — and with the announcement<br>it requires too. See `strings.opensInNewTab`. |
| `strings` | no | `Partial<UrlStrings>` | Overrides the new-tab announcement. |
| `ref` | no | `Ref<HTMLAnchorElement>` |  |

### `Value`

Kind: callable.

```text
(props: ValueProps) => import("react").JSX.Element
```

### `ValueProps`

Kind: interface.

Extends: `SpanProps`.

| member | required | type | description / documented default |
| --- | :-: | --- | --- |
| `children` | no | `ReactNode` |  |
| `emptyLabel` | no | `ReactNode` | Rendered when the value is absent. |
| `size` | no | `TextSize` |  |
| `align` | no | `TextProps["align"]` |  |
| `weight` | no | `TextProps["weight"]` |  |
| `type` | no | `ValueType` |  |
| `truncate` | no | `boolean` | Ellipsises the value at one line. Every flex box in the chain needs `min-width: 0` — see `Text`. |
| `ref` | no | `Ref<HTMLSpanElement>` |  |

### `ValueType`

Kind: type.

`inherit` selects no colour, for a value inside a surface that already sets one — the
over-limit half of a character count, a figure in a toned callout.

```text
Extract<TextProps["type"], "inherit" | "main" | "secondary">
```

## Preview recipes

Preview route: Addresses & places — `/primitive-address`

### Ordered by country

```tsx fragment — excerpt from the live preview; surrounding values are supplied by the application
<Address value={{ line1, city, postalCode, country: "GB" }} />
```

### Inline, for a cell

```tsx fragment — excerpt from the live preview; surrounding values are supplied by the application
<Address value={value} format="inline" />
```

### Missing fields

```tsx fragment — excerpt from the live preview; surrounding values are supplied by the application
<Address value={{ line1: "221B Baker Street", city: "London" }} />
```

### Coordinates

```tsx fragment — excerpt from the live preview; surrounding values are supplied by the application
<Coordinates latitude={48.85837} longitude={2.29448} />
<Coordinates latitude={48.85837} longitude={2.29448} format="dms" />
```

### Email

```tsx fragment — excerpt from the live preview; surrounding values are supplied by the application
<Email value="jane@northwind.example" />
<Email value="raj@northwind.example" display="Raj Patel" />
<Email value={null} />
```

### Phone

```tsx fragment — excerpt from the live preview; surrounding values are supplied by the application
<Phone value="+31 6 1234 5678" />
<Phone value="+1 (555) 010-4417" />
<Phone value={null} />
```

### URL

```tsx fragment — excerpt from the live preview; surrounding values are supplied by the application
<Url value="https://northwind.example/invoices/4417" />
<Url value="https://northwind.example/invoices/4417" external />
<Url value="https://northwind.example" display="Northwind" />
<Url value={null} />
```

### Dates and times

```tsx fragment — excerpt from the live preview; surrounding values are supplied by the application
<DatePrimitive value={WHEN} />
<Time value={WHEN} />
<DateTime value={WHEN} />
<DatePrimitive value={WHEN} pattern="EEEE d MMMM yyyy" />
<DatePrimitive value={null} />
```

### Localised names

```tsx fragment — excerpt from the live preview; surrounding values are supplied by the application
import { de } from "date-fns/locale"

<UIProvider config={{ formatting: { locale: "de-DE" }, dates: { locale: de } }}>
  <DatePrimitive value={when} pattern="EEEE d MMMM yyyy" />
</UIProvider>
```

### date-fns, not hand-rolled Date

```tsx fragment — excerpt from the live preview; surrounding values are supplied by the application
<DateRange start={MARCH_3} end={MARCH_7} />
<DateRange start={MARCH_3} end={APRIL_2} />
<DateRange start={MARCH_3} end={NEXT_JAN} />
<DateRange start={MARCH_3} end={APRIL_2} separator=" to " />
<DateRange start={MARCH_3} end={null} />
```

### RelativeTime

```tsx fragment — excerpt from the live preview; surrounding values are supplied by the application
<RelativeTime value={HOURS_AGO} now={NOW} />
<RelativeTime value={DAYS_AGO} now={NOW} />
<RelativeTime value={MONTHS_AGO} now={NOW} />
<RelativeTime value={null} />
```

### Suffix and precision

```tsx fragment — excerpt from the live preview; surrounding values are supplied by the application
<RelativeTime value={date} now={now} addSuffix={false} />
<RelativeTime value={date} now={now} includeSeconds />
```

### Pass the clock in

```tsx fragment — excerpt from the live preview; surrounding values are supplied by the application
<Duration value={45} />
<Duration value={4520} />
<Duration value={90} from="minutes" />
<Duration value={4520} maxParts={1} />
<Duration value={4520} unitDisplay="short" />
<Duration value={null} />
```

### Money

```tsx fragment — excerpt from the live preview; surrounding values are supplied by the application
<Money amount={1299.5} currency="EUR" />
<Money amount={1299.5} currency="USD" />
<Money amount={-42} currency="GBP" />
<Money amount={0} currency="JPY" />
<Money amount={null} currency="EUR" />
```

### Minor units

```tsx fragment — excerpt from the live preview; surrounding values are supplied by the application
<Money amount={129950} currency="EUR" unit="minor" />
<Money amount={1299} currency="JPY" unit="minor" minorUnitScale={1} />
<Money amount={129950} currency="KWD" unit="minor" minorUnitScale={1000} />
```

### How the amount is written

```tsx fragment — excerpt from the live preview; surrounding values are supplied by the application
<Money amount={1299.5} currency="EUR" formatMode="with-symbol" />
<Money amount={1299.5} currency="EUR" formatMode="with-code" />
<Money amount={1299.5} currency="EUR" formatMode="decimal" />
```

### Two currencies

```tsx fragment — excerpt from the live preview; surrounding values are supplied by the application
<Money
  amount={1299.5}
  currency="EUR"
  secondary={{ amount: 1416.2, currency: "USD" }}
/>
```

### Decided once, not per amount

```tsx fragment — excerpt from the live preview; surrounding values are supplied by the application
<UIProvider
  config={{
    formatting: { locale: "de-DE" },
    money: {
      defaultCurrency: "EUR",
      displayCurrency: "USD",
      dualPricingEnabled: true,
      displayMode: "dynamic",
      layout: "stacked",
    },
  }}
>
  <Money amount={1299.5} secondary={{ amount: 1416.2 }} />
</UIProvider>
```

### Name

```tsx fragment — excerpt from the live preview; surrounding values are supplied by the application
<Name value="Jane McDonald" />
<Name value="  jane   mcdonald " />
<Name value="JANE MCDONALD" force />
<Name value={null} />
```

### Initials

```tsx fragment — excerpt from the live preview; surrounding values are supplied by the application
<Initials value="Jane McDonald" />
<Initials value="Mei Chen" />
<Initials value="Jane McDonald" maxCharacters={1} />
<Initials value="Ana Sofia Reyes" strategy="first-words" />
<Initials value="Ana Sofia Reyes" maxCharacters={3} />
<Initials value="—" fallback="?" />
<Initials value={null} />
```

### Number and percent

```tsx fragment — excerpt from the live preview; surrounding values are supplied by the application
<Number value={1234567.891} />
<Number value={-42} />
<Percent value={0.214} />
<Percent value={1} />
<Percent value={21.4} scaled />
<Number value={null} />
```

### Range

```tsx fragment — excerpt from the live preview; surrounding values are supplied by the application
<Range from={10} to={50} />
<Range from={10} to={50} currency="GBP" />
<Range from={2} to={5} unit="day" />
```

### Ratio

```tsx fragment — excerpt from the live preview; surrounding values are supplied by the application
<Ratio value={3} total={10} />
<Ratio value={3} total={10} format="fraction" />
<Ratio value={3} />
```

### Rating

```tsx fragment — excerpt from the live preview; surrounding values are supplied by the application
<Rating value={4.5} />
<Rating value={4} hideMax />
```

### Quantity

```tsx fragment — excerpt from the live preview; surrounding values are supplied by the application
<Quantity value={1} unit={{ one: "item", other: "items" }} />
<Quantity value={0} unit={ITEM} zeroLabel="no items" />
```

### Measure

```tsx fragment — excerpt from the live preview; surrounding values are supplied by the application
<Measure value={2.5} unit="kilogram" />
<Measure value={2.5} unit="kilogram" unitDisplay="long" />
```

### Dimensions

```tsx fragment — excerpt from the live preview; surrounding values are supplied by the application
<Dimensions width={30} height={20} unit="cm" />
<Dimensions width={30} height={20} depth={12} unit="cm" />
<Dimensions width={1920} height={1080} unit="px" />
<Dimensions width={null} height={null} />
```

### File size

```tsx fragment — excerpt from the live preview; surrounding values are supplied by the application
<FileSize value={512} />
<FileSize value={1_100_000} />
<FileSize value={85_800_000} />
<FileSize value={4_100_000_000} />
<FileSize value={null} />
```

### Which megabyte

```tsx fragment — excerpt from the live preview; surrounding values are supplied by the application
<FileSize value={1_100_000} />                    {/* 1 MB */}
<FileSize value={1_100_000} base="decimal" />    {/* 1.1 MB */}
<FileSize value={1_100_000} base="iec" />        {/* 1 MiB */}
```

### Value

```tsx fragment — excerpt from the live preview; surrounding values are supplied by the application
<Value>Northwind Traders</Value>
<SecondaryValue>Supporting detail</SecondaryValue>
<MutedValue>Quieter still</MutedValue>
<MonoValue>req_8f21c440</MonoValue>
<EmptyValue />
```

### Absent is a state

```tsx fragment — excerpt from the live preview; surrounding values are supplied by the application
<InlineList items={["Alice", "Bob", "Carol"]} />
<InlineList items={["red", "green", "blue"]} join="or" />
<InlineList items={items} max={2} />
```

### The same list, three locales

```tsx fragment — excerpt from the live preview; surrounding values are supplied by the application
<InlineList items={names} locale="en-GB" />
<InlineList items={names} locale="es-ES" />
<InlineList items={names} locale="ja-JP" />
```
